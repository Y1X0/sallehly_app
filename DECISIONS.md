# اصطلاح هذا الملف — إلزامي لكل مُدخَل جديد

كل مُدخَل جديد (`# [TAG]`) يجب أن يحمل قسم `## الخطورة` صريحاً، مباشرة بعد
العنوان — لا تترك الخطورة ضمنية بالسرد فقط. سبب هذا القسم: تدقيق لاحق
(2026-08-30) احتاج تصنيف كل نتيجة (مُصلَحة أو مؤجَّلة) عبر خمس جولات تدقيق
بكلا المستودعين، ووجد أن هذا الملف تحديداً لا يحمل ولا كلمة خطورة صريحة
واحدة بأي مُدخَل — فاضطُرّ التصنيف لاستنتاج الخطورة من صياغة الأثر بالكامل.
لا تكرّر هذه الفجوة. نفس المقياس المُعتمَد بمستودع الخادم (`sallehly`)
بالضبط — أربع درجات فقط:

- **حرج (Critical)** — يعطّل التطبيق بالكامل لكل المستخدمين، أو يفسد بيانات
  مالية/شخصية بشكل يصعب التراجع عنه، أو يمنع مستخدماً عادياً من استخدام
  التطبيق أصلاً بلا أي مخرج.
- **خطير (High)** — يمنع مستخدماً عادياً من إتمام مهمة جوهرية (تسجيل دخول،
  دفع، تواصل) بشكل متكرر أو دائم، أو يكشف بيانات حقيقية، أو يعطّل قناة
  ثقة أساسية (إشعارات، تقارير أعطال) بصمت تام.
- **متوسط (Medium)** — أثر حقيقي لكن محدود النطاق أو التكرار، أو مسألة
  منتج/خصوصية مفتوحة لم تُحسَم بعد.
- **منخفض (Low)** — أثر ضيق جداً (أداة أدمن داخلية، حالة حدّية نادرة)، أو
  تنظيف بلا احتمال استغلال حقيقي.

اكتب الخطورة كخيار واحد من الأربعة أعلاه بالضبط، ثم جملة واحدة تبرّرها.

---

# [SEC-FIX-CTXAWAIT-01] استخدام BuildContext بعد await بلا حراسة — 15 موقعاً

## الخطورة
خطير — أخطر موقعين فعلياً بأكثر شاشة استخداماً بالتطبيق (الدردشة، 6
مواضع) وشاشة الدعم، على شبكات جوال متذبذبة؛ ينهار التطبيق بمنتصف مهمة
جوهرية (إرسال رسالة/صورة/بلاغ) لأي مستخدم ألغى تثبيت الودجت أثناء انتظار
الشبكة.

## الاكتشاف
تدقيق شامل (طُلب صراحة) لكل `State` تحوي `async`/`await` (53 ملفاً فُحصت
بالكامل، لا عيّنة) كشف 15 موقعاً يستخدم `context` (غالباً عبر
`AppLocalizations.of(context)`، وموقع واحد عبر `ScaffoldMessenger.of(context)`
خام) **بعد** `await` بلا أي فحص `mounted`/`context.mounted` يحمي ذلك الاستخدام
تحديداً — رغم أن نفس هذه الملفات تستخدم النمط الصحيح بمواضع أخرى (مثال:
`chat_room_screen.dart`'s `blockUserFlow()`/`unblockUserFlow()`، اللتان
تلتقطان `t` قبل أي `await`).

**الأثر الحقيقي:** لو أُلغي تثبيت الودجت (المستخدم رجع للخلف، أغلق bottom
sheet، إلخ) أثناء انتظار طلب شبكة، ثم فشل ذلك الطلب، محاولة قراءة
`AppLocalizations.of(context)` أو `ScaffoldMessenger.of(context)` على
context غير مثبَّت تعطي استثناءً ("Looking up a deactivated widget's
ancestor is unsafe"). أخطر موقعين فعلياً: `support_screen.dart` (فتح
تذكرة دعم، bottom sheet يُغلَق بسهولة أثناء الإرسال) و`chat_room_screen.dart`
(6 مواضع — إرسال رسالة/صورة/موقع/تسجيل صوتي/بلاغ، أكثر شاشة استخداماً
بالتطبيق، على شبكات جوال متذبذبة).

## نقطة دقيقة — `showErrorSnackBar`/`showSuccessSnackBar` تبدو آمنة وليست كذلك دائماً
`lib/core/widgets/success_feedback.dart`'s `showErrorSnackBar(context, message)`
يفحص `context.mounted` **داخلها** فعلاً. لكن Dart يُقيِّم كل معاملات الدالة
**قبل** استدعائها — فلو كان `message` نفسه تعبيراً يستخدم `context` (مثل
`AppLocalizations.of(context)!.xxx`)، هذا التعبير يُنفَّذ وقد ينهار **قبل**
أن تصل السيطرة أصلاً لفحص `context.mounted` داخل `showErrorSnackBar`. الدالة
لا تحمي معامِلاتها، فقط جسمها هي. **لا تفترض أن تمرير أي شيء عبر
`showErrorSnackBar`/`showSuccessSnackBar` يجعله آمناً تلقائياً — المعامل
نفسه يجب أن يكون بالفعل قيمة مُلتقَطة مسبقاً (متغيّر عادي)، لا تعبيراً حياً
يقرأ `context`.**

## الإصلاح
كل الـ15 موقعاً: نمط واحد ثابت — التقاط `final t = AppLocalizations.of(context)!;`
(أو `final messenger = ScaffoldMessenger.of(context);` بموقع `support_screen.dart`
اليتيم الذي يستخدم `ScaffoldMessenger` خاماً) **مرة واحدة بأول الدالة، قبل
أي `await`**، ثم استخدام هذا المتغيّر المُلتقَط بكل مكان لاحقاً — بما فيه
داخل `catch`. لماذا هذا يكفي رغم تعدّد الـ`await` أحياناً بنفس الدالة (مثال:
`sendLocation()`، `toggleRecord()`): `t`/`messenger` بعد الالتقاط مجرّد
مرجع Dart عادي، لا يلمس `context`/شجرة الودجت مرة أخرى إطلاقاً — قراءته لاحقاً
آمنة دائماً بغضّ النظر عن حالة تثبيت الودجت وقتها، فلا حاجة لالتقاط جديد بعد
كل `await`.

## تحقّق — مسح شامل لنفس الفئة من الأخطاء
بعد الإصلاح، بحث كامل بالمشروع عن أي استدعاء آخر لدالة "آمنة" (`showErrorSnackBar`،
`showSuccessSnackBar`، أو أي `showInfo` محلي بنفس النمط) يستقبل معاملاً يستخدم
`context` مباشرة (`AppLocalizations.of(context)` أو ما شابه) — لم يُعثر على أي
حالة إضافية. كل استخدامات `AppLocalizations.of(context)` المتبقية إما (أ) داخل
`build()` (متزامن، لا خطر إطلاقاً)، أو (ب) بعد فحص `mounted`/`context.mounted`
مباشر يحمي ذلك الاستخدام بالذات بلا أي `await` بينهما.

## نطاق متروك عمداً
هذا المسح مقصور على `AppLocalizations.of(context)`/`ScaffoldMessenger.of(context)`
كوسيطين لدوال "آمنة" — لم يُدقَّق كل استخدام `Theme.of(context)`/`MediaQuery.of(context)`
بنفس الأسلوب الدقيق (رغم أن التدقيق الأوسع لكل استخدام `context` بعد `await`
غطّى 53 ملفاً بالكامل ولم يجد أي حالة من هذه الأنواع). أي اكتشاف مستقبلي من
هذه الفئة يستحق نفس المعالجة الفورية، لا افتراض أن هذا الإصلاح غطّى كل شيء
للأبد.

# [FIX-FCMREFRESH-01] تجدد توكن FCM كان يصل لمسار ميت — الإشعارات تموت بصمت لمستخدم لا يزال مسجَّلاً دخوله

## الاكتشاف
`lib/core/notifications/firebase_notification_service.dart` كان يحمل تطبيقاً
كاملاً موازياً لإرسال توكن FCM للسيرفر (`configure()`/`_dio`/`_baseUrl`/
`_sendTokenToServer()`/`sendPendingToken()`) — لكن `configure()` و
`sendPendingToken()` لم يكن يستدعيهما أي كود بالتطبيق كله (تحقَّق بالبحث
الشامل بـ`lib/` و`test/`). نتيجة ذلك: `_dio`/`_baseUrl` يبقيان `null` دائماً،
فمعالج `_messaging.onTokenRefresh` (المُسجَّل بـ`init()`) كان يحفظ أي توكن
متجدد محلياً فقط (`fcm_token_pending` بـSharedPreferences) ولا يرسله للسيرفر
إطلاقاً — بلا أي خطأ ظاهر، بصمت تام.

تجدد التوكن يحدث فعلياً بالاستخدام الطبيعي (لا فقط عند إعادة التثبيت) —
فمستخدم مسجَّل دخوله بالفعل، توكنه القديم لا يزال مُسجَّلاً بالسيرفر، لكن
جهازه يحمل توكناً جديداً السيرفر لا يعرفه — تصير كل محاولات الإرسال له لاحقاً
تفشل بصمت من طرف Firebase (توكن قديم غير صالح). المسار الحقيقي والوحيد الذي
كان يعمل فعلاً هو `AuthProvider._sendFcmTokenToServer()` — يُستدعى فقط بعد
`login()`/`verifyOtp()`/`loadMe()`، لا عند التجدد.

## الحل
- `AuthProvider` (`lib/providers/auth_provider.dart`) يستمع الآن بنفسه لـ
  `FirebaseMessaging.instance.onTokenRefresh` (بمُنشئه)، ويستدعي نفس
  `_sendFcmTokenToServer()` المُثبَتة أصلاً — تجلب توكناً طازجاً بنفسها
  (`getToken()`) فلا حاجة لتمرير التوكن الجديد من حدث التجدد. `try/catch` حول
  التسجيل نفسه + `onError` صريح على الـ`Stream` (لا فقط داخل
  `_sendFcmTokenToServer`) لأن مجرد الوصول لـ`onTokenRefresh`/الاشتراك فيه قد
  يفشل ببيئة `flutter test` (لا Firebase مُهيَّأ) — نفس السبب الموثَّق أصلاً
  بتعليق `test/models/auth_provider_test.dart`. `dispose()` جديد يُلغي
  الاشتراك.
- `FirebaseNotificationService`: حُذف التطبيق الموازي كاملاً
  (`configure`/`_dio`/`_baseUrl`/`_sendTokenToServer`/`sendPendingToken`/
  `_fetchAndSaveToken`) بدل تركه مساراً ميتاً ثانياً بجانب الحل الجديد —
  `AuthProvider` الآن المصدر الوحيد لإرسال توكن FCM، أولاً وعند كل تجدد.
  استيرادا `dio`/`shared_preferences` أُزيلا من هذا الملف (لم يعودا
  مُستخدَمين فيه إطلاقاً بعد الحذف).

## نطاق متروك عمداً
لم يُشغَّل `flutter analyze`/`flutter test` بهذه الجلسة (لا Flutter SDK متاح
بهذه البيئة) — التغيير روجع يدوياً بعناية (كل استخدام سابق للدوال المحذوفة
تحقَّق بالبحث الشامل أنه غير موجود بأي مكان آخر)، لكن يحتاج تأكيد CI الحقيقي
(`flutter analyze` + `flutter test`) قبل الدمج.

# [FIX-CRASHREPORT-01] لا يوجد أي تقرير أعطال بالتطبيق — كل خطأ حتى الآن اكتُشف بقراءة الكود لا بإشارة فعلية

## الاكتشاف
جولة تدقيق ثالثة (طُلبت صراحة بعد إغلاق ثغرة انتقال حالة الطلب الحرجة) طلبت
تقييماً جديداً كاملاً لكلا المستودعين. أحد أهم بندين استحقّا إصلاحاً فورياً:
`lib/main.dart` لم يكن يحوي أي شبكة أمان لأخطاء وقت التشغيل — لا
`runZonedGuarded`، لا `FlutterError.onError`، لا
`PlatformDispatcher.instance.onError`، ولا أي تبعية تقرير أعطال
(Crashlytics/Sentry/إلخ) بـ`pubspec.yaml` إطلاقاً. الدالة `main()` كانت
تلفّ تهيئة Firebase فقط بـ`try/catch` يتجاوز الإشعارات بصمت عند الفشل، ثم
`runApp()` بلا أي حماية أخرى.

**الأثر الحقيقي:** كل عيّنة (bug) اكتُشفت بهذا المشروع طوال هذه الجلسة —
بما فيها ثغرة حرجة بانتقال حالة الطلب — وصلت عبر قراءة الكود يدوياً، لا عبر
أي إشارة من التطبيق نفسه. لو حدث استثناء غير متوقَّع بيد مستخدم حقيقي (شبكة
سيئة، جهاز غريب، حالة سباق لم تُختبَر)، لا أثر منه يصل لفريق التطوير إطلاقاً
— لا تنبيه، لا سجل، لا شيء. هذا يعني عملياً أن أي عطل بالإنتاج يبقى مجهولاً
حتى يبلّغ عنه مستخدم بنفسه (نادراً ما يحدث) أو يُكتشف صدفة بجولة تدقيق لاحقة.

## الحل
- `pubspec.yaml`: أُضيف `firebase_crashlytics: ^5.3.0` — يستخدم نفس تهيئة
  Firebase الموجودة أصلاً بالمشروع (`firebase_messaging`)، لا مشروع Firebase
  إضافياً ولا إعداد جديد يُطلَب من المستخدم.
- `android/build.gradle.kts` و`android/app/build.gradle.kts`: أُضيف Gradle
  plugin (`com.google.firebase.crashlytics` نسخة `2.8.1`) — يرفع تلقائياً
  ملف `mapping.txt` الخاص بـProGuard/R8 مع كل بناء إصدار (المشروع أصلاً
  `isMinifyEnabled = true`)، حتى تظهر تتبعات الأعطال بأسماء دوال/كلاسات
  حقيقية على Firebase Console بدل رموز مبهمة بلا فائدة. نسخة الإضافة هذه
  ليست تخميناً — تحقَّقت من مطابقتها لما هو مُختبَر فعلياً بمستودع FlutterFire
  الرسمي (استُنسخ للقراءة فقط لتفقّد `settings.gradle` بمشروعهم المرجعي، بعد
  أن حجب البروكسي الوصول المباشر لـ`firebase.google.com`/`dl.google.com`)
  مع نفس إصدار الحزمة `5.3.0` المضاف هنا.
- `lib/main.dart`: أُعيدت كتابته بالكامل حول ثلاث طبقات حماية قياسية بـFlutter:
  1. `FlutterError.onError` — أخطاء إطار Flutter نفسه (استثناء بـ`build()`
     مثلاً).
  2. `PlatformDispatcher.instance.onError` — أخطاء غير متزامنة تهرب من شجرة
     الودجت تماماً (مثال: `Future` غير مُنتظَر يرمي استثناءً). يُعيد `true`
     لمنع الإطار الافتراضي من طباعتها للـconsole فقط، لا لكتم التطبيق ولا
     لإسقاطه.
  3. `runZonedGuarded` كطبقة خارجية أخيرة — يلتقط أي استثناء يهرب حتى من
     الطبقتين أعلاه (مثلاً لو حدث قبل أن تتم تهيئة Firebase أصلاً بمنطقة
     الـ`try` الداخلية). الرفع هنا محمي بـ`try/catch` صريح إضافي لأن
     Crashlytics نفسه قد لا يكون مُهيَّأً بعد بهذه النقطة المبكرة جداً.
  الطبقتان (1) و(2) تُسجَّلان فقط داخل مسار تهيئة Firebase الناجح — لو فشلت
  التهيئة (كما كان يحدث سابقاً أيضاً)، التطبيق يكمل عمله الطبيعي بلا إشعارات
  ولا تقرير أعطال، لا يتعطل.
  `FirebaseCrashlytics.instance.setCrashlyticsCollectionEnabled(!kDebugMode)`
  يمنع تسجيل أعطال التطوير المحلي بلوحة Crashlytics — تُغرق إشارة الأعطال
  الحقيقية من مستخدمين فعليين بضجيج غير ذي قيمة.

## نطاق متروك عمداً
- لم يُشغَّل `flutter analyze`/`flutter test`/بناء Android فعلي بهذه الجلسة
  (لا Flutter SDK متاح بهذه البيئة) — يحتاج تأكيد CI حقيقي قبل الدمج، بما
  فيه خطوة بناء Android لأنها التغيير الوحيد بهذه الجلسة التي تلمس ملفات
  Gradle مباشرة.
  اختبار وحدة تلقائي مباشر لسلوك `runZonedGuarded`/`FlutterError.onError`
  ليس عملياً هنا (يتطلب محاكاة عطل حقيقي على مستوى isolate)؛ الثقة بهذا
  الإصلاح تعتمد على المراجعة اليدوية الدقيقة + كونه نمطاً قياسياً موثَّقاً
  من Flutter نفسها، لا على اختبار آلي جديد.
- لم يُضَف أي إعداد iOS-specific إضافي لـCrashlytics (مثل `dSYM` upload
  scripts بـXcode) — هذا المشروع لم يُبنَ لـiOS بأي جولة سابقة بهذه الجلسة،
  فتُرك هذا الجزء لمن يجهّز أول بناء iOS فعلي.
- لا Sentry ولا أي بديل آخر — Crashlytics اختير لأنه الخيار المجاني الوحيد
  المتكامل مباشرة مع البنية التحتية الموجودة أصلاً (Firebase)، لا لأنه
  الأفضل موضوعياً بكل الحالات.

# [FIX-ADMINPAGINATION-01] دفتر الحساب وسجل عمليات الأدمن مقطوعان عند أول 100 صف — سقف صامت بلا أي إشارة للمستخدم

## الاكتشاف
نفس جولة التدقيق الثالثة: كل من `AdminProvider.loadLedger()` و
`AdminProvider.loadAuditLogs()` كانا يستدعيان `AdminApi.getLedger()`/
`getAuditLogs()` بـ`limit: 100` ثابتاً بلا أي `offset` غير صفري إطلاقاً —
رغم أن الخادم (`routes/admin.routes.js` بمستودع الباك-إند) يدعم
`limit`/`offset` كاملَين (حتى 200) ويُعيد `total` أصلاً، وأن
`AdminApi.getLedger()`/`getAuditLogs()` بهذا المستودع نفسه يحملان معامل
`offset` جاهزاً بتوقيعهما — الفجوة كانت حصراً بطبقة الحالة (`AdminProvider`)
وشاشتي العرض، لا بالخادم ولا بطبقة الـAPI.

**الأثر الحقيقي:** دفتر الحساب المالي وسجل عمليات الأدمن — وكلاهما أدوات
تدقيق مالي/تشغيلي — كانا يعرضان دائماً أحدث 100 صف فقط بصمت تام، بلا أي
مؤشر بالواجهة يوحي بوجود صفوف أقدم غير معروضة. أي منصة نشطة تتجاوز 100 حركة
مالية أو عملية أدمن، سجل التدقيق فعلياً "مقطوع" — لا يمكن لأي أدمن رؤية ما
قبل ذلك دون وصول مباشر لقاعدة البيانات. سجل تدقيق مالي لا يمكن رؤية أكثر من
أحدث 100 صف منه ليس سجل تدقيق فعلياً.

## الحل
نمط "تحميل المزيد" بزر بأسفل القائمة (لا تمرير لانهائي) — تفضيلاً للبساطة
والأمان بشاشة بيانات مالية (تمرير لانهائي بمثل هذه الشاشات يفتح باباً لعناصر
مكررة/مفقودة بحالات سباق التمرير، غير مبرَّر هنا):
- `AdminProvider`: `loadLedger({int? userId})` يحفظ الآن `_ledgerUserId`
  خاصاً به مع كل استدعاء صريح؛ دالة جديدة `loadMoreLedger()` تجلب الصفحة
  التالية بنفس الفلتر المحفوظ (`offset: ledgerEntries.length`)، تحرس ضد
  استدعاء مزدوج أثناء تحميل قائم (`ledgerLoading || ledgerLoadingMore`) وضد
  استدعاء بعد اكتمال كل الصفوف (`ledgerEntries.length >= ledgerTotal`)،
  وتُلحِق النتائج الجديدة بالقائمة القائمة بدل استبدالها. نفس النمط حرفياً
  لـ`loadAuditLogs`/`loadMoreAuditLogs` مع `_auditSearch` بدل `_ledgerUserId`.
  `_adminPageSize = 100` ثابت مشترك بين المسارين (نفس القيمة المستخدمة سلفاً،
  لا تغيير بحجم الصفحة نفسه).
- `admin_ledger_screen.dart`/`admin_audit_screen.dart`: `ListView.separated`
  بكلتا الشاشتين يُضيف صفاً إضافياً بنهاية القائمة فقط إن كان هناك فعلاً
  المزيد (`entries.length < total`) — يعرض إما مؤشر تحميل صغير (أثناء جلب
  الصفحة التالية) أو زر "تحميل المزيد" (`_LoadMoreFooter`، ودجت خاص بكل شاشة
  — نُسخ لا استورد مشترك، الشاشتان لا تتشاركان ملف widgets بعد، واستخلاصه
  لملف مشترك قرار تصميمي أوسع مؤجَّل عمداً، ليس خطأً).
- `lib/l10n/app_ar.arb`/`app_en.arb`: مفتاح ترجمة جديد واحد فقط
  (`adminLoadMoreButton`) لنص الزر، بكلا الشاشتين.
- `test/models/admin_provider_superadmin_test.dart`: 6 اختبارات جديدة —
  لكل من `loadMoreLedger`/`loadMoreAuditLogs`: (أ) يجلب الصفحة التالية
  ويُلحِقها بشكل صحيح، (ب) لا يستدعي أي طلب إضافي لو كل الصفوف محمَّلة أصلاً
  (يُثبَت بـ`verify(...).called(1)` على الاستدعاء الأولي بالذات — لا
  بـ`verifyNever` بمطابقات `any()` واسعة، لأن الاستدعاء الأولي لـ
  `loadLedger()`/`loadAuditLogs()` بإعداد الاختبار نفسه كان سيُطابق أي نمط
  `verifyNever` واسع بغضّ النظر عن صحة `loadMoreLedger()`، فينتج فشل اختبار
  زائف)، (ج) يحافظ على فلتر `userId`/نص البحث عبر استدعاءات "تحميل المزيد"
  المتتالية.

## نطاق متروك عمداً
لم يُشغَّل `flutter analyze`/`flutter test` بهذه الجلسة (لا Flutter SDK متاح
بهذه البيئة) — التغيير روجع يدوياً بعناية، بما فيها إعادة فحص الاختبارات
الجديدة نفسها ذهاباً وإياباً لتصحيح خطأ منطقي بأحدها (`verifyNever` كان
سيفشل زائفاً)، لكن يحتاج تأكيد CI حقيقي (`flutter analyze` + `flutter test`)
قبل الدمج.

# [FIX-OFFERDECISION-01] قبول/رفض عرض بلا catch — فشل الشبكة يترك المستخدم بلا أي إشارة

## الاكتشاف
جولة تدقيق رابعة (طُلبت صراحة، محور مناطق لم تغطِّها الجولات الثلاث
السابقة: صمود التطبيق أمام شبكة سيئة وإجراء لم يصل رده). `RequestsProvider.acceptOffer`/
`rejectOffer` (`lib/features/requests/provider/requests_provider.dart`) كانا
`try { ... } finally { ... }` **بلا `catch` إطلاقاً** — بعكس كل دالة كتابة
أخرى بنفس الملف (`createRequest`، `sendOffer`، `updateRequestStatus`)
التي تلتقط الاستثناء، تُسجّله بـ`error`، ثم تُعيد رميه (`rethrow`) لنفس
النمط دائماً. نقطتا الاستدعاء الوحيدتان
(`lib/features/customer/screens/offers_screen.dart`) لم تكونا تلتقطان أي
شيء هما الأخريان — `acceptOffer` تُستدعى مباشرة من `onPressed` بلا
`try/catch`، و`onReject` بلا حتى `await` داخل دالة منفصلة.

**الأثر الحقيقي:** عميل يضغط "قبول" أو "رفض" على عرض فني أثناء انقطاع شبكة
مؤقت — الزر يتوقف عن التحميل، **بلا أي رسالة، بلا SnackBar، بلا شيء
إطلاقاً**. المستخدم لا يعرف هل نجحت العملية أم فشلت، ولا يوجد أي مؤشر
يدعوه لإعادة المحاولة. الاستثناء نفسه لا يُسقط التطبيق (يُلتقَط بصمت عبر
`runZonedGuarded`/`PlatformDispatcher.instance.onError` المُضافين بـ
`[FIX-CRASHREPORT-01]`، فيصل الآن على الأقل لـCrashlytics) لكن المستخدم
بالواجهة لا يرى أي أثر له.

## الحل
- `requests_provider.dart`: كلا الدالتين صارتا تتبعان نفس نمط `createRequest`
  بالحرف — `catch (e) { error = e is ApiException ? e.message : 'رسالة
  fallback'; rethrow; }` قبل `finally`.
- `offers_screen.dart`: `acceptOffer` (كانت دالة محلية بلا حماية) و`rejectOffer`
  (دالة جديدة، كانت مجرد `await` خام داخل `onReject` مباشرة) صارتا تلتقطان
  `on ApiException catch (e)` (رسالة السيرفر الفعلية) و`catch (_)` (رسالة
  fallback محلَّاة) وتعرضان `showErrorSnackBar` — نفس دالة الطبقة الآمنة
  المستخدَمة بكل الشاشات الأخرى (`SEC-FIX-CTXAWAIT-01` أعلاه). `t` يُلتقَط
  قبل أي `await` بكلا الدالتين، مطابقاً لنفس اتفاقية `SEC-FIX-CTXAWAIT-01`
  بالحرف.
- `lib/l10n/app_ar.arb`/`app_en.arb`: مفتاحان جديدان فقط
  (`offerAcceptFailedMessage`/`offerRejectFailedMessage`)، بنفس نمط
  `offerSendFailedMessage` الموجود أصلاً بجانبهما.

## الإثبات
`test/models/requests_provider_test.dart`: اختباران جديدان (فشل السيرفر
على كل من `acceptOffer`/`rejectOffer`) يثبتان أن `error` يُسجَّل والاستثناء
يُعاد رميه، بالإضافة لاختبار نجاح جديد لـ`rejectOffer` (لم يكن مُختبَراً
إطلاقاً قبل هذا الإصلاح، بعكس `acceptOffer`). **تحقَّق يدوياً أن الاختبارين
الجديدين يفشلان فعلاً بدون الإصلاح** (`git stash` مؤقت للملف، تشغيل —
فشل بـ`Expected: 'تعذر قبول العرض', Actual: <null>` كما هو متوقَّع تماماً
لكلا الاختبارين، ثم استعادة الإصلاح).

هذه الجلسة حمّلت Flutter SDK فعلياً (3.47.2 stable — نفس القناة التي يستخدمها
CI، `channel: stable` بلا تثبيت إصدار بكل ملفات `.github/workflows/*.yml`)
وشغّلت التحقق محلياً بدل الاكتفاء بمراجعة يدوية: `flutter analyze` — صفر
أخطاء (42 تحذير info مسبق الوجود، بلا تغيير). `flutter test` — **281/281**
(كانت 278 قبل هذا الإصلاح، +3 هنا: فشل `acceptOffer`، نجاح+فشل `rejectOffer`)،
باستثناء الفشل الوحيد المعروف مسبقاً وغير المرتبط (`l10n_screenshot_capture_test.dart`،
يتطلّب خطوة تثبيت خطوط عربية حقيقية غير متوفرة بهذه البيئة تحديداً — الاختبار
نفسه يذكر ذلك صراحة برسالة الخطأ).

## نطاق متروك عمداً
لم تُلمَس أي دالة كتابة أخرى بأي provider آخر — هذا الإصلاح مقصور تماماً
على `acceptOffer`/`rejectOffer` تحديداً (المطلوبتين صراحة). مسح شامل لبقية
الـproviders لنفس النمط (`try` بلا `catch`) لم يُطلَب بهذه الجولة ولم
يُجرَ.

# [FIX-SECURESTORAGE-01] عطل FlutterSecureStorage يجعل تسجيل دخول ناجح فعلياً يبدو فاشلاً للأبد

## الاكتشاف
`TokenStorage` كانت تستدعي `FlutterSecureStorage().read/write/delete`
مباشرة بلا أي `try/catch`. عطل Android Keystore الفاسد — موثَّق ومعروف
بحزمة `flutter_secure_storage` نفسها: يحدث فعلياً بعد تغيير قفل الشاشة أو
بصمة الإصبع، استعادة نسخة احتياطية لجهاز آخر، أو ترقية نظام — يجعل كل
قراءة/كتابة لاحقة على ذلك الجهاز تفشل دائماً بـ`PlatformException`
(`KeyStoreException`/`BadPaddingException` بالتحديد على أندرويد)، بلا أي
علاقة بصحة بيانات الدخول المُرسَلة.

المسار الفعلي المتأثر — `AuthProvider.login()`/`verifyOtp()`:
1. `authApi.login()` ينجح فعلياً على الخادم — بيانات الاعتماد صحيحة،
   توكن حقيقي صدر.
2. `_saveSession()` تُستدعى بعدها، وتستدعي `tokenStorage.saveToken()`.
3. إن كان الـKeystore فاسداً، `saveToken()` تُلقي `PlatformException` هنا
   مباشرة.
4. هذا الاستثناء غير `ApiException`، فيسقط بـ`catch (_)` العام بـ`login()`
   ويُحوَّل لرسالة عامة "حدث خطأ غير متوقع" + `rethrow` — المستخدم يرى
   تسجيل دخول فاشلاً رغم أن الخادم صادَق عليه فعلياً بنجاح تام.
5. **الأخطر**: هذا العطل لا يزول بإعادة المحاولة — نفس الجهاز بنفس
   Keystore الفاسد سيفشل بنفس الطريقة لكل محاولة تسجيل دخول لاحقة، للأبد،
   حتى يمسح المستخدم بيانات التطبيق يدوياً (لا يعرف غالباً أن هذا هو الحل
   أو حتى أن المشكلة من جهازه لا من كلمة مروره).

أثر أوسع اكتُشف أثناء التدقيق: `TokenStorage.getToken()` تُستدعى داخل
معترض `ApiClient` (`onRequest`) **بكل طلب API دون استثناء** — لو تُرك
استثناء `PlatformException` هنا بلا التقاط، فكل طلب API لاحق (لا فقط
تسجيل الدخول) كان سيفشل بنفس العطل طالما الـKeystore فاسد، حتى الطلبات
التي لا تحتاج مصادقة إطلاقاً.

## الحل
`TokenStorage` أُعيد بناؤها حول تجريد داخلي صغير (`SecureTokenBackend`)
يسمح بحقن خلفية وهمية للاختبار — بنفس نمط `authApiOverride` المُعتمَد
أصلاً بـ`AuthProvider`؛ الاستخدام الفعلي بالتطبيق (`app.dart`،
`authenticated_media.dart`) لم يتغيّر (`TokenStorage()` بلا أي معامل، يبني
خلفية `FlutterSecureStorage` حقيقية تلقائياً كما كان الحال).

الثلاث دوال المكتوبة/القارئة كلها تلتقط `PlatformException` الآن:
- `saveToken`: عند الفشل → `deleteAll()` (يُصلح الـKeystore الفاسد) ثم
  إعادة محاولة الكتابة مرة واحدة. لو نجحت، تسجيل الدخول يكتمل بنجاح فعلي
  فوراً بنفس المحاولة الأولى، لا يحتاج المستخدم لإعادة أي شيء يدوياً.
- `getToken`: عند الفشل → `deleteAll()` ثم إرجاع `null` (معاملة الحالة
  كـ"لا يوجد توكن محفوظ"، بدل إسقاط الاستثناء على كل طلب API لاحق).
- `clearToken`: عند الفشل → `deleteAll()` (تصل لنفس الحالة النهائية
  المطلوبة — لا يوجد توكن — مع إصلاح التخزين الفاسد بنفس الوقت). هذه تُستدعى
  بأول سطر بـ`login()`/`verifyOtp()` قبل أي اتصال بالخادم أصلاً، فعطل هنا
  كان يُسقط محاولة الدخول قبل أن تبدأ حتى.

`deleteAll()` هي طريقة الشفاء الموثَّقة رسمياً لهذا العطل تحديداً (وليست
مبالغة — `TokenStorage` هي المستخدم الوحيد لـ`FlutterSecureStorage` بكل
التطبيق، فلا خطر على أي بيانات أخرى).

## الإثبات
`test/core/storage/token_storage_test.dart` — ملف جديد، خلفية وهمية
(`_FakeBackend implements SecureTokenBackend`) تُلقي `PlatformException`
عند أول استدعاء فقط (تحاكي بالضبط سلوك العطل الحقيقي: يُصلَح فوراً بعد
`deleteAll`). أربعة اختبارات: `saveToken` تنجح فعلياً رغم فشل أول محاولة
كتابة، `getToken` تُرجع `null` بدل رمي استثناء، `clearToken` لا تُسقط
الاستدعاء، وتدفق كامل (كتابة فاشلة ثم قراءة لاحقة ناجحة بلا أي عطل متبقٍّ).

**تحقَّق يدوياً أن الاختبارات الأربعة تفشل فعلاً بدون الإصلاح** — ليس
بفشل توكيد (assertion) بل بفشل ترجمة كامل (`git stash` مؤقت لـ
`lib/core/storage/token_storage.dart` فقط): `Type 'SecureTokenBackend'
not found` و`No named parameter with the name 'backendOverride'` —
دليل قاطع أن غطاء الأمان (القدرة على حقن خلفية فاشلة للاختبار، والمنطق
الذي يعالج فشلها) لم يكن موجوداً إطلاقاً قبل هذا الإصلاح. استُعيد
الإصلاح بعدها (`git stash pop`) وأعيد التشغيل: **4/4 نجاح**.

هذه الجلسة حمّلت Flutter SDK فعلياً (3.47.2 stable، نفس القناة التي
يستخدمها CI) وشغّلت التحقق محلياً: `flutter analyze` — صفر أخطاء جديدة
(42 تحذير info مسبق الوجود بلا علاقة بهذا الملف، بلا تغيير). `flutter
test` — **المجموعة الكاملة تمر بالكامل باستثناء نفس الفشل الوحيد المعروف
مسبقاً وغير المرتبط** (`l10n_screenshot_capture_test.dart`، يتطلّب خطوة
تثبيت خطوط عربية حقيقية غير متوفرة بهذه البيئة — تحقَّقنا أنه يفشل بنفس
الطريقة بالضبط حتى بدون هذا الإصلاح إطلاقاً، فليس تراجعاً سبَّبناه).

## نطاق متروك عمداً
لم يُدقَّق أي استخدام آخر لتخزين محلي (`shared_preferences` عبر
`AppStorage`، أو أي كاش آخر) بحثاً عن نفس فئة الفشل — `SharedPreferences`
لا تستخدم Android Keystore أصلاً فلا تتعرّض لهذا العطل تحديداً (بلا تشفير،
فلا "مفتاح تشفير فاسد" ممكن)، لذا لم يكن التوسّع لها ذا معنى بنفس الإصلاح.
لم يُضَف أي مؤشر/سجلّ (telemetry) لعدد مرات حدوث هذا العطل فعلياً بالإنتاج
— قد يفيد لاحقاً لمعرفة مدى تكرار المشكلة الحقيقي.

# [FIX-TOGGLEFEEDBACK-01] تبديل حالة مهنة فاشل يمر بصمت — بلا أي رسالة خطأ للأدمن

## الاكتشاف
`AdminMetaScreen`: `onToggle: (e) => admin.toggleService(int.tryParse('${e['id']}') ?? 0, e['is_active'] == 0)`
— استدعاء مباشر بلا `await` ولا `try/catch`. بالمقابل، `onDelete` بنفس
الشاشة يمر عبر `confirmDelete()` التي **هي وحدها** تلتقط الأخطاء وتعرضها
(`showErrorSnackBar`)؛ `AdminProvider.toggleService()` نفسها أيضاً بلا
`catch` (بعكس `toggleUser`/`deleteUser` بنفس الملف، اللذين يضبطان `error`
صراحة عند الفشل).

النتيجة: فشل فعلي (شبكة، 403 صلاحيات، 500 خادم) عند تفعيل/تعطيل مهنة كان
يمر بصمت تماماً — لا `SnackBar`، لا أي مؤشر للأدمن أن حالة المهنة لم تتغيّر
فعلياً بالخادم رغم أن المفتاح (`Switch`) قد يبدو أنه تبدّل بصرياً لحظياً
قبل أن يعيد `loadMeta()`/الحالة الفعلية تصحيحه بصمت أيضاً.

## الحل
دالة جديدة `toggleServiceWithFeedback()` بـ`_AdminMetaScreenState`، بنفس
نمط `try/catch`/`showErrorSnackBar` المُستخدَم أصلاً بـ`confirmDelete()`
و`editService()` بنفس الملف (`on ApiException catch (e) => showErrorSnackBar(context, e.message)`،
`catch (_) => showErrorSnackBar(context, t.editDataFailedMessage)` —
رسالة عامة موجودة أصلاً، بلا إضافة مفتاح ARB جديد). `onToggle` بقائمة
"المهن" يستدعيها الآن بدل `admin.toggleService(...)` المباشرة.

## الإثبات
`test/widgets/admin_toggle_feedback_test.dart` — ملف جديد: `AdminProvider`
بـ`apiOverride: MockAdminApi` (`getMeta`/`getAllServices` ناجحتان بمهنة
واحدة، `toggleService` تُلقي استثناءً)، يبني `AdminMetaScreen` فعلياً،
يضغط `Switch` المهنة، ويتحقّق من ظهور `SnackBar` برسالة الخطأ العامة.

**تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
لـ`admin_meta_screen.dart` فقط): فشل بـ
`Expected: exactly one matching candidate, Actual: Found 0 widgets` —
لا أي رسالة تظهر إطلاقاً، بالضبط العطل الموصوف. استُعيد الإصلاح بعدها،
نجاح الاختبار.

`flutter analyze`: صفر أخطاء جديدة (42 تحذير info مسبق الوجود، بلا علاقة
بهذا التغيير). `flutter test`: **286/286** فعلياً (كانت 285، +1 هنا)
باستثناء نفس الفشل الوحيد المعروف مسبقاً وغير المرتبط
(`l10n_screenshot_capture_test.dart`، خطوط عربية غير متوفرة بهذه البيئة).

## نطاق متروك عمداً
`updateService`/`createService`/`deleteService`/`deletePackage` (نفس
الملف) و`togglePackageActive` (تبويب "الباقات"، سطر ~313) تشترك نفس فئة
الفجوة جزئياً: `AdminProvider` نفسها بلا `catch` يضبط `error`، لكن معظم
مواقع الاستدعاء تمر فعلياً عبر `confirmDelete()`/`addPackage()` اللتين
تلتقطان الخطأ بمستوى الشاشة — **إلا** `togglePackageActive` تحديداً
(`onToggle: (e) => admin.togglePackageActive(e)` بتبويب الباقات) التي
تشترك بالضبط نفس الفجوة غير المُصلَحة هنا (استدعاء مباشر بلا `await`/
`try/catch`). لم يُطلَب صراحةً بهذه الجولة — يُترَك لطلب لاحق إن أُريد،
بنفس نمط الحل المُطبَّق هنا بالضبط.

# [FIX-TOGGLEFEEDBACK-02] نفس علة FIX-TOGGLEFEEDBACK-01 بتبويب الباقات — togglePackageActive بلا await ولا try/catch

## الخطورة
منخفض — أداة أدمن داخلية (لوحة إدارة الباقات، ليست بمواجهة العميل/الفني)،
والأثر مقصور على الأدمن نفسه عدم معرفة أن حالة الباقة لم تتغيّر فعلياً.

## الاكتشاف
موثَّق مسبقاً كنطاق متروك عمداً بـ`FIX-TOGGLEFEEDBACK-01` أعلاه:
`AdminMetaScreen`، تبويب "الباقات": `onToggle: (e) =>
admin.togglePackageActive(e)` — استدعاء مباشر بلا `await` ولا `try/catch`،
بعكس `toggleServiceWithFeedback()` التي أُصلحت لتبويب "المهن" بنفس الجولة.
`AdminProvider.togglePackageActive()` تستدعي `updatePackage()` الداخلية
التي **تُعيد رمي (`rethrow`)** أي فشل فعلياً (بعد ضبط `error` صراحة) — لكن
بلا أي مستدعٍ يلتقط ذلك الرمي هنا، فيمر الفشل بصمت تماماً بالواجهة رغم
وصوله فعلياً للطبقة الأعلى.

## الحل
دالة جديدة `togglePackageWithFeedback()` بـ`_AdminMetaScreenState`، بنفس
النمط الحرفي المستخدَم بـ`toggleServiceWithFeedback()` بالضبط (`on
ApiException catch (e) => showErrorSnackBar(context, e.message)`، `catch
(_) => showErrorSnackBar(context, t.editDataFailedMessage)`). `onToggle`
بتبويب "الباقات" يستدعيها الآن بدل `admin.togglePackageActive(e)` المباشرة.

## الإثبات
`test/widgets/admin_toggle_feedback_test.dart` — اختبار ثانٍ جديد بنفس
الملف: `AdminProvider` بـ`apiOverride: MockAdminApi` (`getMeta` ناجحة
بباقة واحدة، `updatePackage` تُلقي استثناءً)، يبني `AdminMetaScreen`
فعلياً، يبدّل لتبويب "الباقات"، يضغط `Switch` الباقة، ويتحقّق من ظهور
`SnackBar` برسالة الخطأ العامة (نفس مفتاح ARB الموجود أصلاً، بلا إضافة جديد).

**تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
لـ`admin_meta_screen.dart` فقط): فشل بـ`Expected: exactly one matching
candidate, Actual: Found 0 widgets` — نفس العطل الموصوف بالضبط، والاختبار
الأول (تبويب المهن) بقي ناجحاً (يثبت أن الإصلاحين مستقلان). استُعيد
الإصلاح بعدها، نجاح كلا الاختبارين.

`flutter test`: **287/287** فعلياً (كانت 286، +1 هنا) باستثناء نفس الفشل
الوحيد المعروف مسبقاً وغير المرتبط (`l10n_screenshot_capture_test.dart`،
خطوط عربية غير متوفرة بهذه البيئة — تحقَّق مباشرة أنه يفشل بنفس الرسالة
حتى على نسخة نظيفة قبل أي تعديل بهذه الجلسة).

# [FIX-DEADFIELD-02] تنظيف رموز خطأ خادمية أصبحت غير قابلة للوصول — إزالة ميزة "الفني الموجَّه مباشرة"

## الخطورة
منخفض — تنظيف كود ميت بحت، بلا أي أثر سلوكي على أي مستخدم.

## الاكتشاف
راجع `DECISIONS.md` بمستودع `sallehly` (`[FIX-DEADFIELD-02]`): ميزة "إنشاء
طلب موجَّه مباشرة لفني معيّن" (`technician_id` بجسم `POST /requests`) أُزيلت
بالكامل من الخادم — قرار منتج (لم تكن مستخدمة من أي شاشة بهذا التطبيق
إطلاقاً، ولا فائدة من إبقائها بلا استخدام حقيقي). هذا ترك رمزَي خطأ خادميين
(`REQUEST_TECHNICIAN_UNAVAILABLE`، `REQUEST_DIRECT_TO_OTHER_TECHNICIAN`) لا
يُرسِلهما الخادم مستقبلاً إطلاقاً، لكن مورّد رموز الخطأ (`api_error_codes.dart`)
لا يزال يتعرّف عليهما — كود ميت غير قابل للوصول، لا يُستدعى أبداً.

## الحل
حُذفت حالتا `switch` المقابلتان لكلا الرمزين من `api_error_codes.dart`.
عبارات ARB المرتبطة (`apiErrorRequestTechnicianUnavailable`،
`apiErrorRequestDirectToOtherTechnician`) لم تُحذَف عمداً — إزالة عبارات
ARB تلمس ملفات ترجمة متعددة (عربي/إنجليزي) وكوداً مولَّداً، تكلفة/مخاطرة
أعلى بكثير من فائدة تنظيف عبارتين يتيمتين لا أثر عملي لبقائهما.

## الإثبات
`flutter analyze lib/core/api/api_error_codes.dart`: صفر أخطاء. لا اختبار
سلوكي جديد — لا فرق سلوكي قابل للاختبار بين "الحالة موجودة لكن غير قابلة
للوصول أبداً" و"الحالة محذوفة" (الخادم لا يرسل هذين الرمزين بأي حال بعد
إزالتهما هناك، مُثبَت باختبارات الخادم نفسها). المجموعة الكاملة للعميل
تبقى **287/287** كما هي — هذا التغيير لا يمس أي مسار مُختبَر.

# [SEC-FIX-COORDMASK-01] تدقيق العميل — أثر إخفاء الإحداثيات على الفني قبل قبول عرضه

## الخطورة
غير محسومة (لا فحص أمني ولا خطأ برمجي) — تدقيق تأكيدي، لا إصلاح. النتيجة:
لا أثر إطلاقاً.

## المطلوب فحصه
راجع `DECISIONS.md` بمستودع `sallehly` (`[SEC-FIX-COORDMASK-01]`) — الخادم
أصبح يرجع `lat`/`lng: null` لفني قدّم عرضاً لكن لم يُقبَل بعد (بدل الإحداثيات
الحقيقية سابقاً). طُلب التحقق صراحة: هل يتعامل تطبيق العميل بأمان مع هذه
القيمة الآن `null` بدل رقم حقيقي كما كانت تصل سابقاً؟

## النتيجة — لا أثر إطلاقاً، الحقل غير مُستخدَم بالتطبيق أصلاً
بحث شامل (`grep` لكل استخدام لـ`lat`/`lng` بكل ملفات `lib/`) يثبت أن
`RequestModel.fromJson` (`lib/models/request_model.dart`) **لا يقرأ عمودَي
`lat`/`lng` إطلاقاً** — لا يوجد حقلان `lat`/`lng` بالنموذج نفسه من الأساس.
لا خريطة (`flutter_map`/`google_maps_flutter`) بأي مكان بالمشروع
(`pubspec.yaml` لا يحمل أي مكتبة خرائط)، ولا أي وصول مباشر لـ`json['lat']`
خارج النموذج بأي شاشة. `geolocator` (المكتبة الوحيدة ذات الصلة بالموقع)
تُستخدَم حصراً بميزة "إرسال الموقع" داخل الشات (`chat_room_screen.dart`) —
تلتقط موقع الجهاز **الحيّ وقت الإرسال** وترسله كرسالة `[location]lat,lng`
منفصلة تماماً، لا علاقة لها بعمودَي `requests.lat`/`requests.lng`.

**الخلاصة:** `lat`/`lng` بجدول `requests` كانا دائماً حقلاً "اكتب فقط" من
منظور هذا التطبيق — يُرسَلان عند إنشاء الطلب (من موقع الجهاز الحيّ وقتها،
عبر شاشة إنشاء الطلب)، يُخزَّنان بالخادم، لكن لا تُقرَآن ولا تُعرَضان بأي
شاشة إطلاقاً حالياً (لا للعميل ولا للفني). بما أن القيمة لا تُقرَأ أصلاً،
تحوّلها من رقم حقيقي إلى `null` بردود معيّنة لا يغيّر أي سلوك مرئي — لا خطر
تعطّل، لا شاشة تعتمد على افتراض وجودها.

## نطاق متروك عمداً
لا تعديل على أي كود — لا حاجة له. لو أُضيفت مستقبلاً شاشة تعرض موقع الطلب
على خريطة (ميزة غير موجودة اليوم)، يجب حينها مراعاة أن الفني غير المؤكَّد
سيستلم `null` عمداً — قرار تصميم مستقبلي، لا شيء يلزم فعله الآن.

# [FIX-CRASHREPORT-IOS-01] إعداد Crashlytics لـiOS — محجوب على شرط مسبق، لا مؤجَّل

## الخطورة
منخفض — Android (المنصة الفعلية الوحيدة المنشورة اليوم) مُغطّاة بالكامل
عبر `FIX-CRASHREPORT-01`؛ هذا يخصّ منصة لم يُبنَ لها التطبيق ولو مرة واحدة.

## الفرق بين "محجوب" و"مؤجَّل" — لماذا هذا التصنيف تحديداً
`FIX-CRASHREPORT-01` وثّق هذه النقطة أصلاً كـ"نطاق متروك عمداً" — صياغة قد
تُقرَأ خطأً على أنها "لم يُعطَ أولوية بعد" أو "تم تخطّيها بقرار". هذا غير
دقيق: الشرط الأساسي لإتمام هذه المهمة **غير متوفّر أصلاً**، لا مسألة وقت أو
أولوية. التمييز مهم — بند مؤجَّل ينتظر دوره بقائمة عمل، بند محجوب ينتظر
شرطاً مسبقاً خارج نطاق أي عمل برمجي ممكن حالياً.

## الشرط المسبق غير المتوفّر (تأكيد مباشر من صاحب المنتج)
1. **لا جهاز Mac** — بناء iOS (`flutter build ios`، `pod install`، فتح
   Xcode) يتطلّب macOS حصراً؛ هذه الجلسة نفسها (بيئة Linux) لا يمكنها حتى
   تجربة أي تعديل على مشروع iOS.
2. **لا حساب Apple Developer** — تسجيل تطبيق iOS بمنصة Firebase، وتوقيع أي
   بناء iOS فعلي، يتطلّبان عضوية Apple Developer Program (مدفوعة).
3. **لا بناء iOS تم إطلاقاً بهذا المشروع** — تحقَّق مباشرة (`FIX-CRASHREPORT-01`
   وهذه الجلسة): لا `ios/Podfile` موجود (يُولَّد تلقائياً بأول
   `pod install`/بناء)، ولا `GoogleService-Info.plist` (يتطلّب تسجيل تطبيق
   iOS بلوحة Firebase Console أولاً — ملف مرتبط بمشروع Firebase الحقيقي،
   بنفس فئة `BACKUP_GITHUB_TOKEN`/keystore التوقيع: لا يجوز لأي وكيل توليده
   نيابة عن صاحب الحساب).

## لماذا لم يُحاوَل تعديل جزئي بلا تحقق
جزء واحد من الإعداد (خطوة رفع `dSYM` عبر Xcode build phase بملف
`ios/Runner.xcodeproj/project.pbxproj`) هو كود/إعداد بحت يمكن كتابته من أي
بيئة نظرياً — لكن هذا الملف هش جداً للتعديل اليدوي بلا Xcode للتحقق (خطأ
واحد قد يُفسد بنية مشروع Xcode بالكامل بصمت)، ولا معنى لإضافته بمعزل عن
باقي الشروط (1) و(2) غير المتوفّرة — إعداد جزئي غير قابل للتحقق أسوأ من عدم
الإعداد إطلاقاً. لم يُنفَّذ.

## شرط الإغلاق
هذا البند يُفتَح للعمل فقط بعد توفّر: جهاز Mac (أو بيئة CI macOS)، حساب
Apple Developer فعّال، وتسجيل تطبيق iOS بلوحة Firebase Console الحالية
(تُنتج `GoogleService-Info.plist`). عندها فقط تصبح خطوات `FIX-CRASHREPORT-01`
المتروكة (رفع `dSYM`، `Podfile`) قابلة للتنفيذ والتحقق الفعليين.

# [SEC-FIX-AUDIOAUTH-01] رسائل الشات الصوتية تُشغَّل من express.static عام بلا أي مصادقة

## الخطورة
عالٍ — نفس فئة الثغرة المُغلَقة أصلاً لصور الشات (`SEC-FIX-UPLOADS-01`
بمستودع الخادم)، لكن تركت مفتوحة للصوت تحديداً. أي طرف يعرف/يخمّن مسار ملف
صوتي بمجلد `/uploads/audios/...` كان يقدر يسمعه مباشرة بلا أي جلسة تسجيل
دخول — تسريب محتوى محادثات خاصة (قد تتضمن تفاصيل شخصية/عنوان/اتفاق سعر
بصوت المستخدم).

## الاكتشاف
`ChatBubble.playAudio()` (القديم) كان يستدعي
`player.play(UrlSource(_mediaUrl(widget.message.audioUrl)))` مباشرة —
`audioplayers`'s `UrlSource` **لا يدعم إرفاق أي هيدرز إطلاقاً** (تحقَّق من
توثيق الحزمة وكودها المصدري مباشرة، `audioplayers-6.8.1/lib/src/source.dart`:
`UrlSource` تحمل `url`/`mimeType` فقط، لا حقل هيدرز). بالمقابل، صور الشات
(`_ImageMessage`) تُعرَض عبر `Image.network(url, headers: ...)` الذي **يدعم**
هيدرز — لهذا أُصلحت الصور بـ`SEC-FIX-C1`/`SEC-FIX-UPLOADS-01` ونُسيت
الرسائل الصوتية رغم أنها تمر بنفس `express.static('/uploads', ...)` العام
بالخادم.

## الحل
`playAudio()` أُعيدت كتابتها بالكامل: بدل تمرير الرابط مباشرة لـ`UrlSource`
(الذي يذهب للخادم بلا أي هيدر)، تُجلَب بايتات الملف كاملة أولاً عبر طلب
`Dio` صريح مصادَق (`authHeadersForMediaUrl(url)` — نفس الآلية المشتركة
المُستخرَجة أصلاً لصور الشات، تُرفق `Authorization: Bearer <JWT>` فقط لو
كان الرابط يعود فعلاً لخادمنا)، ثم تُشغَّل من الذاكرة مباشرة عبر
`BytesSource(bytes)` بدل `UrlSource`. البايتات تُخزَّن بحالة الودجت
(`_audioBytes`) لتفادي إعادة الجلب عند كل ضغط تشغيل/إيقاف مؤقت للرسالة
نفسها. أثناء الجلب تظهر `CircularProgressIndicator` بدل أيقونة التشغيل
(حالة جديدة `_loadingAudio`، تُعطّل الضغط المتكرر أيضاً)، وأي فشل بالجلب أو
بالتشغيل (كلاهما داخل `try/catch` واحد الآن) يُظهر رسالة خطأ جديدة
(`playAudioFailedMessage`، أُضيفت لملفي `app_ar.arb`/`app_en.arb`) بدل
فشل صامت.

منفذ اختبار وحيد (`ChatBubble.debugAudioDio`, `@visibleForTesting`) أُضيف
للودجت العام — `Dio()` كانت تُنشأ مباشرة بداخل `playAudio()` بلا أي طريقة
لاستبدالها من اختبار؛ الحقل الثابت `null` دائماً بالتطبيق الفعلي (لا يغيّر
أي سلوك إنتاجي)، ويُستخدَم فقط لحقن عميل `Dio` بمحوّل وهمي (`HttpClientAdapter`
مخصّص) بالاختبارات، بدل إرسال طلبات حقيقية لخادم الإنتاج
(`https://sallehly.com` — قيمة `AppConfig.baseUrl` الافتراضية الفعلية).

## الإثبات
`test/widgets/chat_bubble_audio_auth_test.dart` — ملف جديد، 4 اختبارات:
1. رسالة صوتية من خادمنا: طلب الجلب يحمل `Authorization: Bearer <JWT>`
   الصحيح على الرابط الكامل الصحيح.
2. رابط صوتي خارجي منتحَل (نفس مسار الاستغلال الذي عولج بصيغة الوسائط
   المنتحَلة بالخادم) — **لا** يُرفق أي هيدر Authorization إطلاقاً، بالضبط
   كصور الشات.
3. أثناء الجلب: مؤشر تحميل يظهر، والضغط المتكرر يُعطَّل (`InkWell.onTap`
   يصبح `null`).
4. فشل الجلب: تظهر رسالة الخطأ الجديدة، ولا يبقى مؤشر التحميل عالقاً للأبد.

**قرار تصميم متعمَّد بشأن ما لا تختبره هذه الاختبارات:** لا يوجد اختبار
يتحقق من نجاح `AudioPlayer.play()` فعلياً (أي وصول `playing == true`).
تحقَّق مباشرة أثناء كتابة الاختبار (تجربة فعلية، ليست افتراضاً): استدعاء
`player.play(...)` بأي مصدر بيئة `flutter test` بلا محاكاة قناة
`xyz.luan/audioplayers` **يتعلّق للأبد** (لا يرمي استثناءً) — لأن
`AudioPlayer.setSourceBytes/Url` تنتظر حدث `"prepared"` عبر `EventChannel`
مُخصَّص لكل مشغّل (`xyz.luan/audioplayers/events/<uuid عشوائي>`) لن يصل
أبداً بلا تطبيق أصلي حقيقي — محاكاته الكاملة تتطلب اعتراض القناتين
(Method + Event) بمعرّف مشغّل عشوائي يتغيّر كل تشغيل، تعقيد غير متناسب مع
الفائدة. هذا متسق مع قرار سابق موجود أصلاً بالمشروع:
`test/widgets/chat_bubble_key_test.dart` لا يضغط تشغيل الصوت إطلاقاً لنفس
السبب بالضبط. الثغرة الأمنية المُصلَحة هنا (غياب أي مصادقة على طلب الجلب)
مُثبَتة بالكامل عبر الاختبارات 1-2 (اللتين تلتقطان الطلب الفعلي المُرسَل
قبل أي محاولة تشغيل) — وهذا هو الجزء الذي كان مفقوداً كلياً بالكود القديم
(لم يكن يُرسَل أي طلب `Dio` إطلاقاً، `UrlSource` تذهب مباشرة للخادم).

**تحقَّق يدوياً أن الاختبارات تفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
لـ`chat_bubble.dart`/`app_ar.arb`/`app_en.arb`): فشل تحميل الملف بخطأ ترجمة
(`Setter not found: 'debugAudioDio'`) — الحقل الجديد غير موجود بالكود
القديم، بالضبط كما هو متوقع (الكود القديم لا يستدعي `Dio` إطلاقاً فلا يوجد
ما يُختبَر). استُعيد الإصلاح بعدها، 4/4 نجاح.

`flutter analyze`: صفر مشاكل على الملفات المعدَّلة/الجديدة. `flutter test`
الكامل: كل الاختبارات تمر باستثناء نفس الفشل الوحيد المعروف مسبقاً وغير
المرتبط (`l10n_screenshot_capture_test.dart`، خطوط عربية غير متوفرة بهذه
البيئة).

## نطاق الخادم المرتبط
`/uploads/audios/...` بالخادم لا يزال يمر عبر `express.static('/uploads', ...)`
العام — هذا الإصلاح (جانب العميل) لا يمنع الوصول المباشر غير المصادَق
لملف صوتي لمن يعرف/يخمّن مساره الدقيق، فقط يوقف العميل الرسمي عن الاعتماد
عليه بلا هيدر. الإغلاق الكامل (حماية `/uploads/audios/:filename` بمسار
محمي حقيقي بالخادم، ثم إزالة `express.static('/uploads', ...)` العام
نهائياً) يُعالَج بمستودع الخادم — راجع `DECISIONS.md` هناك.

# [SEC-FIX-CHATCLEAR-01] بيانات حساب سابق (رسائل شات، طلبات/عروض، رصيد، بيانات أدمن، تذاكر دعم) تبقى بالذاكرة بعد تسجيل الخروج

## الخطورة
خطير — يكشف بيانات حقيقية (نص رسائل شات فعلي قد يحمل عناوين/تفاوض سعر،
سجل شحن رصيد، بيانات كل مستخدمي المنصة لحساب أدمن) لأي مستخدم تالٍ يستخدم
نفس الجهاز، بصمت تام بلا أي مؤشر لأي طرف.

## الاكتشاف
جولة تدقيق سادسة. `app.dart`'s `authProvider.onLoggedOut`/`onAuthenticated`
يمسحان `NotificationProvider` فقط (`[CRIT-FIX-02]`) — الفكرة الأساسية
لذلك الإصلاح ("لا تترك بيانات المستخدم الذي خرج جالسة بالذاكرة حتى دخول
التالي") لم تُطبَّق على أي Provider آخر يخبّئ بيانات خاصة بحساب: `ChatProvider`
(`_messagesByRequest` — **نص رسائل الشات الفعلي**)، `RequestsProvider`
(`requests`/`offers`)، `WalletProvider` (`topups`/`ledger` — سجل مالي)،
`AdminProvider` (`users`/`auditLogs`/`violations`/`complaints`/...)،
`SupportProvider` (`tickets`/`messages`). لا واحد من الخمسة يملك `clear()`
قبل هذا الإصلاح.

**السيناريو الأخطر:** `ChatRoomScreen.build()` يقرأ `chatProvider.messagesFor(requestId)`
**بشكل متزامن** — `initState()` يبدأ `loadMessages(requestId)` داخل
`Future.microtask` فقط، فأول إطار يُرسَم من الكاش الحالي قبل اكتمال أي
تحميل جديد. جهاز مشترك (عائلة، جهاز فني بشركة، حسابا اختبار): حساب أ يفتح
شاتاً لطلب رقم 7 (عنوان، رقم هاتف بالتفاوض)، يسجّل خروجه، حساب ب يدخل
ويفتح شاتاً لطلب رقم 7 آخر تماماً بنفس المعرّف مصادفة — يرى رسائل حساب أ
الفعلية للحظة قبل اكتمال التحميل الجديد.

## الحل
`clear()` جديدة بكل Provider من الخمسة، تُصفّر الحقول الخاصة بحساب فقط —
البيانات المرجعية العامة (`meta`، `packages`، `paymentMethods`، `services`
بلوحة الأدمن) تبقى كما هي عمداً (لا فائدة أمنية من تفريغها، فقط تهدر
استدعاء شبكة إضافي). الخمسة تُستدعى الآن من `app.dart` بكلا الموضعين
(`onAuthenticated` **و** `onLoggedOut`، نفس منطق `[CRIT-FIX-02]`: تسجيل
دخول قد يكون فعلياً تبديل حساب بلا مرور صريح بمسار خروج).

`ChatProvider` تحديداً أُضيف لها أيضاً عدّاد جيل (`_generation`)، نفس نمط
`NotificationProvider._generation` (`[HIGH-FIX-STALEGEN-01]`) بالضبط —
يحمي من رد شبكة متأخر لحساب سابق (بدأ قبل `clear()`) يصل بعد `clear()`
ويُعيد ملء الحالة بصمت لصالح الحساب الجديد. بقية الأربعة اكتفت بـ`clear()`
بسيطة بلا عدّاد جيل — نطاق متروك عمداً، راجع تعليق "نطاق متروك عمداً" أدناه.

## الإثبات
`test/models/chat_provider_test.dart` (مجموعة `clear` جديدة، اختباران):
`clear()` تُفرّغ الرسائل/المحادثات، ورد متأخر لطلب بدأ قبل `clear()` لا
يُعيد ملء الحالة (يثبت حماية عدّاد الجيل). `test/models/requests_provider_test.dart`
(اختبار جديد) و3 ملفات اختبار جديدة (`wallet_provider_clear_test.dart`،
`admin_provider_clear_test.dart`، `support_provider_clear_test.dart`) —
كل واحد يثبت `clear()` تُفرّغ الحقول الخاصة بحساب فقط، وتُبقي القوائم
المرجعية العامة كما هي.

**تحقَّق يدوياً أن كل الاختبارات تفشل فعلاً بدون إصلاحها** (`git stash`
مؤقت لكل ملف Provider على حدة): فشل تحويل (`Error: The method 'clear' isn't
defined`) لكل الخمسة — الدالة غير موجودة أصلاً بالكود القديم. استُعيدت
الإصلاحات بعدها، نجاح تام. المجموعة الكاملة: **300/300** فعلياً (باستثناء
نفس الفشل الوحيد المعروف مسبقاً وغير المرتبط، `l10n_screenshot_capture_test.dart`).

## نطاق متروك عمداً
لم يُضَف عدّاد جيل (`_generation`) لـ`RequestsProvider`/`WalletProvider`/
`AdminProvider`/`SupportProvider` — فقط `clear()` بسيطة. السباق الذي يحميه
عدّاد الجيل (رد شبكة متأخر لحساب سابق يصل بعد `clear()`) أضيق بكثير من
"البيانات تبقى للأبد" الذي يعالجه `clear()` نفسها، ولم يُثبَت تكراره
الفعلي لهذه الأربعة بنفس وضوح `ChatProvider` (المُختبَر أعلاه صراحة). لو
ظهر دليل تكرار فعلي لاحقاً، يُطبَّق نفس نمط `_generation` بسهولة — البنية
جاهزة (`HIGH-FIX-STALEGEN-01`/هذا الإصلاح كلاهما مرجع مباشر).

# [SEC-FIX-COMPLETEREQUEST-01] زر "إنهاء الطلب" بشاشة تفاصيل طلب العميل بلا try/catch — فشل الشبكة يمر بصمت

## الخطورة
متوسط — إجراء حقيقي (تأكيد إنهاء الخدمة، خطوة قبل الدفع/التقييم) يفشل
بصمت تام؛ ليس دائماً (إعادة الضغط قد تنجح لاحقاً)، لكن لا أي إشارة للمستخدم
بينما ينتظر — نفس فئة `FIX-OFFERDECISION-01` (قبول/رفض عرض) المُصلَحة سابقاً
بنفس الملف تقريباً، فُقدت بموقع واحد إضافي لم يشمله ذلك الإصلاح.

## الاكتشاف
جولة تدقيق سادسة. `CustomerRequestDetailsScreen`'s زر "إنهاء الطلب"
(`onPressed`) يستدعي `provider.completeRequest(request.id)` مباشرة بلا
`try/catch` — بعكس الزر الشقيق تماماً بنفس الملف ("إلغاء الطلب"،
`_confirmAndCancel`) الذي يلتقط `ApiException`/أي استثناء آخر ويعرض
`SnackBar`. `completeRequest` → `updateRequestStatus`
(`requests_provider.dart`) تُعيد رمي الاستثناء (`rethrow`) بتصميم — بلا
`try/catch` بالمستدعي، فشل شبكة حقيقي (مثال: خادم الإنتاج المجاني يستيقظ
ببطء، راجع `FIX-CONNECTIVITY-01`) يوقف دوران الزر بلا أي رسالة ولا انتقال؛
المستخدم لا يعرف هل اكتمل الطلب فعلاً أم لا.

## الحل
نفس نمط `_confirmAndCancel` بالضبط: `try`/`on ApiException catch`/`catch`
حول الاستدعاء، مفتاح ARB جديد `completeRequestFailedMessage`.

## الإثبات
`test/widgets/complete_request_feedback_test.dart` — ملف جديد، نفس نمط
`admin_toggle_feedback_test.dart` (`[FIX-TOGGLEFEEDBACK-01]`): `RequestsApi`
مموَّهة تُلقي استثناءً عند `updateStatus`، الشاشة الحقيقية تُبنى، الزر
يُضغَط، يُتحقَّق من ظهور رسالة الخطأ وبقاء الشاشة مفتوحة (لا `Navigator.pop`
إلا عند النجاح).

**تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
للشاشة وملفات ARB): فشل تحويل (`completeRequestFailedMessage` غير معرَّفة).
استُعيد الإصلاح بعدها، نجاح تام.

# [SEC-FIX-OPENLOCATION-01] فتح رسالة موقع بالشات بلا معالجة فشل launchUrl

## الخطورة
منخفض — حالة حدّية نادرة (جهاز أندرويد مقيَّد/مصغَّر بلا أي تطبيق خرائط أو
متصفح)، لا مسار استخدام شائع.

## الاكتشاف
`ChatBubble.openLocation()` كان يستدعي `launchUrl(...)` مباشرة بلا أي
تحقق من نتيجتها ولا `try/catch` — بعكس بقية معالجات الفشل بنفس الملف
(`playAudio`، مُصلَحة بـ`SEC-FIX-AUDIOAUTH-01`). توثيق `url_launcher`
الرسمي نفسه يوضّح أن `launchUrl` قد **ترمي استثناءً أو تُرجع `false`
بهدوء**، حسب نوع الفشل — كلاهما كان يمر بصمت تام هنا.

## الحل
فحص القيمة المُعادة (`false` → رسالة خطأ) **و**`try/catch` معاً (يغطيان
حالتي الفشل كلتيهما)، بنفس نمط `showErrorSnackBar` المستخدَم أعلاه بنفس
الملف. مفتاح ARB جديد `openLocationFailedMessage`.

## الإثبات
`test/widgets/chat_bubble_open_location_test.dart` — ملف جديد. بيئة
`flutter test` لا تحمل قناة منصّة أصلية لـ`url_launcher` (بلا محاكاة)، فأي
استدعاء `launchUrl` يفشل حتماً (`MissingPluginException`) — هذا بالضبط ما
يثبته الاختبار: الفشل يظهر للمستخدم الآن. **ملاحظة تقنية**: رد القناة
(حتى الفاشل) لا يكتمل بمجرد `pump()` عادي بهذه البيئة تحديداً (تحقَّق
تجريبياً بتصحيح مباشر أنه يبقى معلَّقاً للأبد بدونه) — الاختبار يستخدم
`tester.runAsync()` لجولة حدث حقيقية واحدة، خلافاً لبقية اختبارات هذا
الملف (راجع أيضاً تعليق مشابه بـ`chat_bubble_audio_auth_test.dart` عن
قناة `audioplayers` — سلوك مختلف قليلاً لقناة مختلفة، لكن نفس الدرس: لا
افتراض سلوك موحّد لكل القنوات غير المُموَّهة).

**تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
للودجت وملفات ARB): فشل تحويل (`openLocationFailedMessage` غير معرَّفة).
استُعيد الإصلاح بعدها، نجاح تام.

# [SEC-FIX-IMGHEADERCACHE-01] `AuthenticatedNetworkImage` قد يُرفق هيدر Authorization لرابط سابق مع طلب رابط مختلف تماماً

## الخطورة
منخفض — غير قابل للاستغلال فعلياً اليوم (تحقَّق مباشرة: نقطتا الاستخدام
الوحيدتان صور مفردة بشاشات `Navigator.push` تُنشئ `State` جديدة دائماً، لا
عناصر قائمة)، لكنه فجوة تصميم كامنة تصبح خطيرة (تسريب توكن جلسة لخادم
خارجي) فور استخدام هذا الودجت داخل أي قائمة بلا `ValueKey` مستقبلاً.

## الاكتشاف
`_AuthenticatedNetworkImageState` يجلب هيدرز المصادقة مرة واحدة فقط بـ
`initState` ويخزّنها بالحالة — بلا `didUpdateWidget`. لو أعاد Flutter
استخدام نفس الـ`State` لودجت بـ`url` مختلف (إعادة استخدام العناصر ضمن
قائمة بلا مفتاح ثابت — نفس فئة الخطأ التي وثّقها `[FIX-CHATBUBBLE-01]`
لسبب مختلف بودجت شقيق)، تبقى `_headers` القديمة (`Authorization: Bearer
<توكن>` صادرة لرابط أ) وتُرفَق مع طلب `Image.network` لرابط ب الجديد —
حتى لو كان رابط ب خارجياً بالكامل. بالضبط نفس فئة الخطر التي
`isFirstPartyMediaUrl()` (`core/utils/authenticated_media.dart`) صُمِّمت
لمنعها، متجاوَزة عبر كاش حالة قديم لا فحص مفقود.

## الحل
`didUpdateWidget` جديدة: عند تغيّر `widget.url`، تُصفَّر `_headers` وتُعاد
الجلب. حماية إضافية (عدّاد رابط مطلوب صريح، لا عدّاد جيل عام — كافٍ هنا
لأن نطاق الودجت واحد فقط): رد جلب متأخر لرابط سابق لا يُطبَّق لو تغيّر
`url` مجدداً أثناء الانتظار.

## الإثبات
`test/widgets/authenticated_network_image_test.dart` — ملف جديد. يبني
الودجت برابط من خادمنا (يُتحقَّق من هيدر `Authorization` الصحيح)، ثم يعيد
بناءه **بنفس الـState** (بلا `key`) برابط خارجي كلياً — يتحقق أن الهيدر
لا يُرفَق مع الطلب الجديد إطلاقاً.

**تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
للودجت): فشل فعلي — `Authorization: Bearer fake-jwt-token` القديم يصل
فعلاً مع طلب الرابط الخارجي الجديد. استُعيد الإصلاح بعدها، نجاح تام.

# [SEC-FIX-DEEPLINKCLEAR-01] `FirebaseNotificationService.pendingDeepLink` لم يُمسَح عند تسجيل الخروج

## الخطورة
منخفض — أثر ملحوظ محدود (تبديل تبويب غير مطلوب، لا تسريب محتوى مباشر —
راجع تفصيل السيناريو أدناه)، لكنه نفس فئة "بيانات حساب سابق تبقى بالذاكرة"
التي عولجت بجهد فعلي بالجولة السادسة (`SEC-FIX-CHATCLEAR-01`) لخمسة
Providers — هذا السادس (ليس Provider، متغيّر ثابت مستقل) فاته ذلك الإصلاح.

## الاكتشاف
جولة تدقيق سابعة. `FirebaseNotificationService.pendingDeepLink`
(`ValueNotifier` ثابت على مستوى العملية بأكملها، لا مرتبط بأي `Provider`)
يُضبَط عند الضغط على أي إشعار (`_handleNotificationTap`)، ويُستهلَك مرة
واحدة من أي `Layout` (عميل/فني/أدمن) يُبنى لاحقاً. `app.dart`'s
`onLoggedOut`/`onAuthenticated` (المُحدَّثان بـ`SEC-FIX-CHATCLEAR-01`) لا
يمسحانه إطلاقاً. **السيناريو:** حساب أ يستلم إشعاراً (مثال: `type: 'chat'`)،
يُبقي التطبيق بالخلفية بلا ضغط الإشعار فعلياً، ثم يسجّل خروجه؛ حساب ب يدخل
على نفس الجهاز — `Layout.initState()`'s استهلاك الإشعار المعلَّق (بعد أول
إطار) يستهلك حمولة حساب أ القديمة، فيُنقَل ب تلقائياً لتبويب لم يطلبه هو
(مثال: تبويب الدردشات). الأنواع المدعومة (`chat`/`request`/`support`)
كلها تُنقّل لمؤشر تبويب أو تعيد قراءة حالة **حية حالية** (`SupportProvider.openTicket`
الخاص بالحساب الجديد ب نفسه، لا محتوى حساب أ) — فلا تسريب محتوى مباشر،
فقط تنقّل غير مرغوب.

## الحل
`FirebaseNotificationService.pendingDeepLink.value = null;` أُضيف لكلا
موضعَي `app.dart` (`onAuthenticated`/`onLoggedOut`)، بنفس القائمة المُنظَّفة
لباقي بيانات الحساب.

## الإثبات
لا اختبار مخصَّص — نفس القرار المُتَّخذ صراحة لسلك `NotificationProvider.clear()`
الأصلي (`CRIT-FIX-02`) بـ`app.dart`: توصيل hook بسيط بدالة موجودة أصلاً
ومُختبَرة (أو، هنا، `ValueNotifier` قياسي من Flutter SDK نفسه)، لا منطق
جديد يستحق اختباراً مستقلاً. التحقّق تم بمراجعة الكود المباشرة (السطر
الجديد يقع بالضبط بجانب استدعاءات `clear()` الخمسة الأخرى المُختبَرة أصلاً،
بنفس النمط بالضبط).

# [SEC-FIX-BGHANDLERCATCH-01] `firebaseBackgroundHandler` بلا try/catch — فشل صامت تام خارج شبكة FIX-CRASHREPORT-01

## الخطورة
منخفض — نادر التفعيل (يتطلب فشل مكوّن الإشعارات المحلية تحديداً على جهاز/
نظام تشغيل مُعيَّن)، لكنه الفجوة الوحيدة المتبقية بتغطية `FIX-CRASHREPORT-01`.

## الاكتشاف
جولة تدقيق سابعة. `firebaseBackgroundHandler` (نقطة دخول isolate خلفية
FCM، `@pragma('vm:entry-point')`، top-level خارج الكلاس) يستدعي
`FirebaseNotificationService._showLocalNotificationStatic(message)` بلا
أي `try/catch`. هذا الـisolate يعمل **خارج** `main()`'s `runZonedGuarded`/
`FlutterError.onError` (المُضافة بـ`FIX-CRASHREPORT-01`) بالكامل — تلك
الشبكة الآمنة تُنشَأ فقط داخل zone عملية `main()` نفسها، لا تشمل isolate
منفصلاً كهذا. فشل حقيقي بمكوّن الإشعارات المحلية أثناء كون التطبيق
بالخلفية/مغلقاً كان يعني: لا يظهر الإشعار، **ولا يصل أي تقرير عطل
لـCrashlytics أيضاً** — الفجوة الوحيدة بهدف الاستثمار الكامل بالتبليغ عن
الأعطال (`FIX-CRASHREPORT-01` نفسها).

## الحل
`try/catch` حول الاستدعاء، مع `debugPrint` محمي بـ`kDebugMode` عند الفشل
(لا محاولة إرسال لـCrashlytics من هذا الـisolate المعزول — خارج نطاق هذا
الإصلاح الضيق، ونفس فلسفة "فشل لا يُسقط شيئاً آخر" المتَّبعة بكل هذا الملف).

## الإثبات
`test/widgets/firebase_background_handler_test.dart` — ملف جديد.
`RemoteMessage` (من `firebase_messaging`، `const` قابلة للإنشاء المباشر
بلا تهيئة Firebase) تُمرَّر لـ`firebaseBackgroundHandler` مباشرة. بيئة
`flutter test` بلا Firebase حقيقي مُهيَّأ ولا قناة منصّة لـ
`flutter_local_notifications` — الاستدعاء الداخلي لـ`_localNotifications.show()`
يفشل حتماً (تحقَّق تجريبياً: `LateInitializationError` عند عدم تهيئة
`FlutterLocalNotificationsPlatform.instance`)، وهذا بالضبط ما يثبته
الاختبار: `expectLater(firebaseBackgroundHandler(message), completes)`.

**تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
للملف): فشل فعلي — `LateInitializationError` يصل لخارج الدالة، يُلتقَط
كاستثناء غير مُعالَج بإطار الاختبار. استُعيد الإصلاح بعدها، نجاح تام.

# مراجعة — عرض العميل لبيانات `GET /chats` الخام لا يحتاج تعديلاً (الإصلاح الجذري بالخادم)

## الخطورة
لا ينطبق — لا كود تغيّر بهذا المُدخَل، توثيق فقط.

## السبب
جولة تدقيق سابعة راجعت `lib/models/chat_summary_model.dart` و
`lib/features/chat/screens/chats_screen.dart`'s `_previewText()`: يعرضان
`last_body`/`other_name` القادمين من `GET /chats` كما هما، بلا أي تحقق
إضافي من طرف العميل أن `requestId` يخص المستخدم الحالي فعلاً. هذا صحيح،
لكنه **ليس ثغرة إضافية على العميل** — هو ببساطة السطح الذي كان سيُظهر أثر
`SEC-FIX-CHATSCOPE-04` (تسريب `GET /chats` بالخادم) لو بقي مفتوحاً. بما
أن ذلك الإصلاح مُغلَق فعلاً (ومُدقَّق بـ`SEC-FIX-CHATSCOPE-CONSOLIDATION-01`
كحارس حصري وحيد الآن)، لا يوجد سيناريو حالي يصل فيه `last_body`/`other_name`
لمستخدم لا يستحقهما — فلا حاجة لفحص عميل إضافي مكرِّر (فلسفة "لا تصميم
لحاجة افتراضية لا سيناريو حقيقي لها اليوم" المتَّبعة بكل هذا المشروع).
لو ظهر مستقبلاً تسريب خادم مماثل، الإصلاح الصحيح هناك (نفس ما حدث ثلاث
مرات سابقاً)، لا طبقة تصفية عميل إضافية تخفي العرض دون معالجة السبب.

# [SEC-FIX-CTXAWAIT-02] استخدام `context` الخام كمعامل بعد `await` بلا حراسة — 64 موقعاً، 18 ملفاً

## الخطورة
خطير — نفس فئة `SEC-FIX-CTXAWAIT-01` وبنفس المنطق: انهيار فعلي (وليس فقط
استثناء مُلتقَط) لأي مستخدم ألغى تثبيت الودجت (رجع للخلف، أغلق dialog/bottom
sheet) أثناء انتظار شبكة فشلت لاحقاً — يشمل مجدداً أكثر شاشات التطبيق
استخداماً (الدردشة، الآن 8 مواضع بدل الافتراض الأولي 6) بالإضافة لتسجيل
الدخول/التسجيل والشاشات الإدارية.

## الاكتشاف
مراجعة مباشرة لمصدر Flutter SDK نفسه
(`packages/flutter/lib/src/widgets/framework.dart`، حوالي السطر ٩٤٩) كشفت
أن `State.context` getter:
```dart
BuildContext get context {
  assert(() { if (_element == null) throw FlutterError(...); return true; }());
  return _element!;
}
```
الجزء `assert(...)` (برسالة الخطأ المفيدة) وضع تصحيح فقط — لكن `return
_element!;` نفسه يرمي استثناء **بكل أوضاع التشغيل** (بما فيها الإنتاج)
بمجرد أن يُصفِّر `State.dispose()`/`unmount()` قيمة `_element`. أي هذا
يعني أن حتى **مجرد قراءة `context` كمعامل خام** (بلا أي استدعاء `.of(context)`
عليه) يرمي استثناءً على `State` مُزالة — بشكل مستقل عن، وقبل، أي شيء تفعله
الدالة المُستدعاة داخلياً بـ`context.mounted`.

`SEC-FIX-CTXAWAIT-01` (أعلاه) أصلَح فقط معامل **`message`** بنداءات
`showErrorSnackBar(context, message)`/`ScaffoldMessenger.of(context)` (بالتقاط
`t`/`messenger` قبل أي `await`) — لم يحمِ عمداً معامل **`context`** الخام
نفسه بنفس تلك النداءات (راجع "نطاق متروك عمداً" بذلك الإدخال). هذه بالضبط
الفجوة التي غطّتها الجولة السابعة: مسح شامل جديد بحث عن كل نداء
`showErrorSnackBar(context, ...)`/`showSuccessSnackBar(context, ...)` داخل
`catch` بلا `if (!mounted) return;`/`if (!context.mounted) return;` كأول
سطر فيها — 64 موقعاً بـ18 ملفاً.

**اكتشاف إضافي غير متوقَّع أثناء التنفيذ:** أربعة مواضع بـ`chat_room_screen.dart`
(`blockUserFlow()`، `unblockUserFlow()`، `reportConversationFlow()`،
`reportMessageFlow()`) كانت مذكورة سابقاً (بإدخال `SEC-FIX-CTXAWAIT-01`)
كأمثلة "صحيحة" مرجعية — وهي كذلك فعلاً بخصوص معامل الرسالة (تلتقط `t` قبل
`await`). لكن عند الفحص المباشر لهذه المرة، الحارس `if (!mounted) return;`
الموجود بهذه الدوال يقع داخل مسار نجاح الـ`try` (بعد `await` مباشرة، يحمي
`showInfo(...)`)، **وليس** داخل كتلتي الـ`catch` أنفسهما — فلا يحمي إطلاقاً
نداءات `showErrorSnackBar(context, ...)` بالـ`catch`. نفس فئة الخطأ
بالضبط، فقط بموضع مختلف داخل نفس الدالة.

## الحل
نمط ميكانيكي واحد ثابت بكل الـ64 موقعاً: إضافة `if (!mounted) return;`
كسطر أول حرفي داخل كل كتلة `catch` تستخدم `context` خاماً بلا حراسة سابقة
تحديداً لها (الشاشات المبنية على `StatelessWidget` استُثنيت بحكم عدم وجود
`State.mounted` أصلاً — تُستخدم فيها `context.mounted` بدلاً منه، وتحقَّق
أنها مضبوطة بشكل صحيح مسبقاً بكل الحالات المفحوصة).

بـ`toggleRecord()` تحديداً (`chat_room_screen.dart`)، الحارس الجديد أُدرج
كأول سطر بكل كتلة `catch` — **قبل** `recordingTimer?.cancel()` الموجودة
مسبقاً بنفس الكتلة. هذا آمن: `dispose()` (نفس الملف) يستدعي
`recordingTimer?.cancel()` مستقلاً بكل الأحوال، فلا تسريب مؤقّت لو خرجت
الدالة مبكراً بحالة عدم التثبيت.

**ملف إضافي لم يكن بالقائمة الأولية**: `lib/features/settings/screens/edit_profile_screen.dart`
— عُثر عليه بمسح واسع على مستوى `lib/` كاملاً، وليس فقط بالملفات
المرشَّحة مسبقاً؛ يؤكد أهمية عدم الاعتماد على قائمة مُعدَّة مسبقاً بلا
إعادة تحقق مستقلة بمهام بهذا الحجم.

## تحقّق — مسح شامل ومراجعة يدوية لكل ملف
التنفيذ الميكانيكي (64 موقعاً) نُفِّذ عبر وكيل فرعي بتوجيه دقيق (يشمل آلية
الخطأ بالضبط مع استشهاد بسطر SDK، نمط الإصلاح المطلوب بمثال، تحذير صريح
بعدم الوثوق بالقائمة المُزوَّدة مسبقاً بل إعادة التحقق ذاتياً). بعد اكتماله،
راجعتُ شخصياً `git diff` لكل ملف من الـ20 (18 + الملفين الإضافيين بنفس
الجولة) للتأكد أن كل إدراج مطابق تماماً للنمط المطلوب (سطر أول حرفي
بالضبط داخل كتلة `catch` فعلية غير محروسة) وأنه لا يوجد أي تعديل آخر خارج
هذا النطاق. تحقَّقت تحديداً من أمان حالة `toggleRecord()`/`recordingTimer`
الموصوفة أعلاه بقراءة مباشرة لـ`dispose()`.

`flutter analyze` بعد الإصلاح: صفر أخطاء جديدة (نفس ٢٠ ملاحظة `info`
موجودة مسبقاً بالمشروع، تحقَّقتُ أن لا واحدة منها بسطر أضفناه). `flutter
test` الكامل: 300+ اختبار، فشل واحد فقط
(`test/widgets/l10n_screenshot_capture_test.dart`، متغيّر بيئة
`ARABIC_FONT_PATHS` غير مضبوط بهذه البيئة تحديداً — تحقَّقتُ أنه فاشل
بنفس الشكل بدون أي من تغييرات هذه الجولة أيضاً عبر `git stash`، غير مرتبط
إطلاقاً).

**لا اختبار تشغيلي مخصَّص جديد لهذه الفئة تحديداً** (نفس سابقة
`SEC-FIX-CTXAWAIT-01` الأصلية — تحقَّق بالبحث: صفر نتائج لـ"CTXAWAIT" بمجلد
`test/`). السبب نفسه: إعادة إنتاج هذا الخطأ فعلياً بـ`flutter_test` تتطلب
إزالة تثبيت ودجت بمنتصف عملية `await` حقيقية مع تشكيلة تبعيات (مزوّدات،
Socket.IO، `ScrollController`) معقّدة بما يكفي لجعل اختبار كهذا هشاً وعالي
الخطر مقابل قيمة تحقّق محدودة على نمط ميكانيكي بسيط أصلاً ومُراجَع سطراً
بسطر. الثقة هنا مبنية على: (أ) مطابقة الاكتشاف الأصلي لآلية SDK موثَّقة
بالمصدر مباشرة لا افتراضاً، (ب) مراجعة شخصية لكل موقع من الـ64 عبر `git
diff`، (ج) `flutter analyze`/`flutter test` الكاملين بلا انحدار.

## نطاق متروك عمداً
هذا المسح مقصور على `catch` تستخدم `context` كمعامل مباشر بنداءات
`showErrorSnackBar`/`showSuccessSnackBar`/`ScaffoldMessenger.of(context)` —
لم يُراجَع كل استخدام آخر لـ`context` (مثل `Navigator.of(context)`) خارج
هذا النمط تحديداً بنفس الدقة، رغم أن الوكيل الفرعي أكّد أن الأمثلة
الأخرى المفحوصة (`StatelessWidget`، `settings_screen.dart`/`support_screen.dart`
بالتقاط `messenger` مسبقاً) صحيحة أصلاً. أي اكتشاف مستقبلي بنفس الفئة
يستحق نفس المعالجة الفورية.

# [SEC-FIX-CTXLINT-01] `use_build_context_synchronously` مفعَّلة لكن غير قاتلة بـCI — رفعها لـerror كشف بقّتين حقيقيتين إضافيتين

## الخطورة
خطير — نفس فئة `SEC-FIX-CTXAWAIT-01`/`02` بالضبط. لكن الأهم من موقعين
جديدين هنا: هذا إصلاح **بنيوي**، لا موقعي — بدونه، أي موقع ٦٥ مستقبلي من
نفس الفئة يمر بصمت تام عبر CI مهما كان عدد جولات التدقيق اليدوي.

## السبب — سؤال مباشر بعد الجولة السابعة
بعد أن أظهرت الجولة السابعة أن التدقيق اليدوي الأول (`SEC-FIX-CTXAWAIT-01`،
١٥ موقعاً) وصف أربعة مواقع بـ`chat_room_screen.dart` بأنها "صحيحة" بينما
حارسها كان بمكان خاطئ فعلياً، السؤال المباشر كان: هل `use_build_context_synchronously`
(قاعدة lint رسمية بمكتبة `package:flutter_lints`، مصمَّمة خصيصاً لهذه الفئة
من الأخطاء) مفعَّلة أصلاً؟ ولو كانت مفعَّلة، هل تفشل CI فعلاً أم تمر بصمت؟

## الاكتشاف
القاعدة مفعَّلة فعلاً (جزء من `package:flutter_lints/flutter.yaml`
المُضمَّن بـ`analysis_options.yaml`) — لكنها، كبقية قواعد lint، بseverity
افتراضية `INFO`. وسير عمل `.github/workflows/android-build.yml` يشغّل
`flutter analyze --no-fatal-infos` **عمداً** (بتعليق موثِّق بالكود يشرح
السبب: هناك ~١٦ ملاحظة `info` أسلوبية أخرى غير متعلقة بالأمان —
`unnecessary_underscores`، `deprecated_member_use`، `use_null_aware_elements`،
`unnecessary_import` — لا يستحق أي منها فشل CI). النتيجة العملية: القاعدة
كانت **موجودة ومفعَّلة وتظهر بمخرجات `flutter analyze` محلياً — لكنها لم
تكن تفشل CI إطلاقاً منذ إنشاء هذا السير**، رغم كونها بالضبط القاعدة التي
كانت ستمنع فئة `SEC-FIX-CTXAWAIT-01`/`02` كاملة لو كانت قاتلة من البداية.

**رفع severity هذه القاعدة تحديداً (قبل أي إصلاح مواقع) كشف فوراً ٤ مواقع
حقيقية لم يغطِّها أي من مسحي `SEC-FIX-CTXAWAIT-01`/`02` اليدويين:**

1. `chat_room_screen.dart:172` — `sendLocation()`: `showErrorSnackBar(context,
   t.enableGpsMessage)` بعد `await Geolocator.isLocationServiceEnabled()`
   بلا فحص `mounted` — **بمسار try العادي، ليس catch**. كلا المسحين
   السابقين استهدفا كتل `catch` تحديداً؛ هذا الموقع خارج نطاقهما بالكامل.
2. نفس الملف، سطر ١٩٤ — بعد `await Geolocator.checkPermission()`/
   `requestPermission()`، نفس النمط بالضبط.
3. نفس الملف، سطر ٢١٢ — بعد `await Geolocator.getCurrentPosition()`/
   `getLastKnownPosition()`، نفس النمط بالضبط.
4. `admin_support_chat_screen.dart:133` — `if (mounted) Navigator.pop(context);`
   داخل `onPressed` بـ`build(BuildContext context)`. هذا مختلف بشكل دقيق:
   `context` هنا معامل محلي بـ`build()` (وليس `State.context` المضمَّن)،
   و`mounted` يفحص `State.mounted` — متغيّران مختلفان بنظر محلل التدفق، رغم
   تطابق قيمتهما وقت التشغيل. رسالة المحلل نفسها تحدِّد الحل: "Guard a
   'State.context' use with a 'mounted' check on the State, and other
   BuildContext use with a 'mounted' check on the BuildContext".

هذا يثبت نقطة الجولة السابعة حرفياً: التدقيق اليدوي المتكرر (حتى بجولتين
متتاليتين مخصَّصتين لنفس الفئة) له سقف — أداة آلية مُفعَّلة بشكل صحيح
تجده مباشرة.

## الحل
جزءان لا يتجزّآن:

**١. الإصلاحات الموضعية (٤ مواقع):** `if (!mounted) return;` كسطر أول قبل
استخدام `context` بالمواقع الثلاثة الأولى (نفس نمط `SEC-FIX-CTXAWAIT-02`).
الموقع الرابع مختلف: `if (mounted) Navigator.pop(context);` →
`if (context.mounted) Navigator.pop(context);` — استخدام `context.mounted`
(الامتداد على `BuildContext` نفسه) بدل `mounted` (حقل `State`) لأن
`context` هنا هو معامل `build()` المحلي تحديداً.

**٢. الإصلاح البنيوي (هو الأهم):** بـ`analysis_options.yaml`:
```yaml
analyzer:
  errors:
    use_build_context_synchronously: error
```
هذا يرفع severity هذه القاعدة تحديداً لـ`error`، فتبقى قاتلة رغم
`--no-fatal-infos` (الذي يُسكِت `INFO` فقط) — بلا التأثير على أي من الـ~١٦
قاعدة أسلوبية الأخرى التي لا تزال `INFO` وتُسكَت كما هي بنفس الفلاغ. لم
تُحذَف `--no-fatal-infos` نفسها ولم يُلغَ نطاقها بالكامل عمداً: ذلك كان
سيُلزم إصلاح الـ١٦ ملاحظة الأسلوبية غير المرتبطة فوراً لفتح CI، توسيع
نطاق لا علاقة له بالسؤال المطروح. تعليق موضّح أُضيف بكلا الملفين
(`analysis_options.yaml` وسير `android-build.yml`) يوثِّق هذا القرار
لمن يقرأ الكود لاحقاً ويتساءل لماذا استثناء واحد وسط قائمة إعفاءات.

## الإثبات
**فشل قبل الإصلاح (تحقَّق تجريبياً):** `git stash` مؤقت للمواقع الأربعة
فقط (مع إبقاء رفع الـseverity)، ثم `flutter analyze --no-fatal-infos` —
فشل فعلي، الأربعة مواقع تظهر بـ`error` صراحةً (لا `info`)، exit code `1`.
```
error • ... • lib/features/admin/screens/admin_support_chat_screen.dart:133:44 • use_build_context_synchronously
error • ... • lib/features/chat/screens/chat_room_screen.dart:172:27 • use_build_context_synchronously
error • ... • lib/features/chat/screens/chat_room_screen.dart:194:27 • use_build_context_synchronously
error • ... • lib/features/chat/screens/chat_room_screen.dart:212:27 • use_build_context_synchronously
20 issues found.
```
استُعيدت المواقع الأربعة بعدها. **نجاح بعد الإصلاح:** `flutter analyze
--no-fatal-infos` — صفر مواقع `use_build_context_synchronously`، ١٦ ملاحظة
`info` أسلوبية متبقية فقط (لا علاقة لها)، exit code `0`. `flutter test
--exclude-tags screenshot` (بالضبط أمر CI): 301 اختبار، نجاح تام، بلا
انحدار.

## نطاق متروك عمداً
هذا الإصلاح يرفع severity قاعدة **واحدة** فقط. لم يُراجَع ما إذا كانت
قواعد lint أخرى ذات قيمة أمنية مشابهة (مثال: `avoid_dynamic_calls`،
`unawaited_futures`) موجودة بمجموعة `flutter_lints` ومفعَّلة لكن غير
قاتلة أيضاً — هذا خارج نطاق السؤال المطروح بهذه الجولة (كان مخصَّصاً
لفئة `context`/`await` تحديداً)، لكنه يستحق نفس السؤال مستقبلاً: "مفعَّلة
أم قاتلة فعلاً؟" لكل قاعدة ذات قيمة أمنية حقيقية، لا افتراض أن التفعيل
وحده كافٍ.

# [SEC-FIX-DOUBLESUBMIT-01] "تعطيل الزر أثناء الإرسال" كان حماية واجهة فقط — لا حارس فعلي داخل الحالة، فضغطة مزدوجة سريعة تُرسل العملية مرتين

## الخطورة
متوسط — أثر حقيقي وقابل للإثبات (وليس افتراضياً)، لكن نافذة الاستغلال
ضيقة جداً (أسرع من إطار رسم واحد، ~16ms — أضيق بكثير من فارق زمن ضغطتين
بشريتين متتاليتين المعتاد) فلا يحدث "بشكل متكرر أو دائم" لمستخدم عادي.
العواقب عند حدوثه أيضاً محدودة الخطورة (طلب مكرَّر يظهر بقائمة الطلبات،
قرار عرض مكرَّر يرفضه الخادم على الأرجح لأن العرض الثاني وصل لحالة غير
"pending" أصلاً، طلب شحن رصيد مكرَّر يراجعه أدمن قبل أي اعتماد فعلي) — لا
إفساد بيانات لا يمكن التراجع عنه ولا تعطّل كامل.

## الاكتشاف
جولة تدقيق مخصَّصة لفئة أخطاء "يبدو حامياً بالكود لكنه لا يحقق الحماية
فعلياً" (نفس الفئة التي غطّتها `SEC-FIX-LIKEESCAPE-01` بمستودع الخادم،
وفئة `SEC-FIX-CTXAWAIT-01`/`02`/`SEC-FIX-CTXLINT-01` بهذا المستودع تحديداً
— تلك الفئة الأخيرة مغلقة ومحروسة بـCI الآن، لم تُعَد مراجعتها). أحد
البنود المطلوب فحصها صراحة: "أي زر 'قيد الإرسال بالفعل، تجاهل الضغطات
الإضافية' — هل الزر يُعطَّل فعلاً أثناء الاستدعاء غير المتزامن، أم توجد
نافذة تسمح بضغطة مزدوجة؟"

فحص كل زر بالتطبيق يُعطَّل بمنطق `loading ? null : onPressed` (17 ملفاً)
كشف أن **الحماية الوحيدة تعيش بطبقة الواجهة فقط**: `notifyListeners()`
بـFlutter/`provider` لا يُعيد رسم الودجت فوراً بنفس السطر — يُجدوِل إعادة
الرسم فقط (`Element.markNeedsBuild`) لتُنفَّذ بالإطار **التالي**. أي دالة
كتابة بطبقة الحالة (Provider) كانت تبدأ بـ`_setLoading(true)` ثم تُكمل حتى
أول `await` **بلا أي تحقق مسبق من أنها قيد التنفيذ أصلاً** — فاستدعاء
الدالة نفسها مرتين متتاليتين بلا `await` بينهما (بالضبط ما يحدث فعلياً لو
وصلت ضغطتان على نفس الزر قبل أن ينفَّذ إطار الرسم الذي يُعطِّله بصرياً)
كان يُنفِّذ كلا الاستدعاءين حتى النهاية — طلبان حقيقيان منفصلان يصلان
للخادم، لا طلب واحد كما يوحي مظهر الزر "المعطَّل".

الفئة الوحيدة المحصَّنة أصلاً بهذا المشروع: `ChatProvider` (`sendMessage`/
`sendLocation`/`sendAudio`/`sendImage`) — تبدأ كل دالة فيها بـ
`if (sending) return;` **قبل** أي شيء آخر، حارس حقيقي داخل طبقة الحالة
نفسها، مستقل تماماً عن توقيت إعادة رسم الواجهة. هذا التباين تحديداً (نمط
صحيح موجود أصلاً بملف واحد، غائب عن كل الملفات الأخرى المشابهة) هو ما
يجعل هذا الاكتشاف مطابقاً بدقة لفئة "يبدو حامياً لكنه ليس كذلك دائماً" —
الشكل الظاهر للزر (`loading ? null : onPressed`) مطابق تماماً بكل مكان،
لكن الحماية الفعلية غائبة إلا حيث أُضيف حارس صريح إضافي.

الملفات المتأثرة فعلياً (دوال كتابة حقيقية بمعنى تُغيّر حالة بالخادم، لا
قراءات):
- `RequestsProvider` (`lib/features/requests/provider/requests_provider.dart`):
  `createRequest`، `sendOffer`، `acceptOffer`، `rejectOffer`،
  `updateRequestStatus` (ومنها `completeRequest`)، `rateRequest`،
  `submitComplaint`، `cancelRequest` — أخطرها عملياً `createRequest`
  (طلب خدمة مكرَّر) و`acceptOffer`/`rejectOffer` (قرار عرض مزدوج).
- `WalletProvider.submitTopup` (`lib/features/wallet/provider/wallet_provider.dart`)
  — إجراء مالي-الطابع (طلب شحن رصيد بإيصال).
- `SupportProvider.createTicket`/`sendMessage` (`lib/features/support/provider/support_provider.dart`).

**لماذا لم يُستخدَم `loading` نفسه كحارس مباشرةً** (الحل "الأبسط" ظاهرياً):
`loading` بـ`RequestsProvider`/`WalletProvider` مُشترَك أيضاً مع دوال
قراءة (`loadRequests`/`loadWallet`/...). حارس ساذج (`if (loading) return;`)
على دالة كتابة كان سيُعرِّض لخطأ **أسوأ** من الأصل: لو كانت قراءة خلفية
(مثال: `loadRequests` غير صامتة قيد التنفيذ بشاشة أخرى بنفس التطبيق) قائمة
صدفة باللحظة نفسها التي يضغط فيها المستخدم "قبول عرض"، الحارس الساذج كان
سيتجاهل ضغطة القبول الحقيقية **بصمت تام** — فشل صامت جديد أخطر من ضغطة
مزدوجة نادرة. لذا حارس مخصَّص منفصل بكل دالة كتابة (`_creatingRequest`،
`_sendingOffer`، `_decidingOffer` — مشترك بين `acceptOffer`/`rejectOffer`
عمداً لأنهما يستدعيان نفس نقطة نهاية القرار لنفس العرض، `_updatingStatus`،
`_ratingRequest`، `_submittingComplaint`، `_cancelingRequest`) — لا يتقاطع
مع أي دالة قراءة إطلاقاً، بنفس فلسفة `ChatProvider.sending` المُعتمَدة
أصلاً (منفصل عن `loading` القارئ بنفس الملف).

`AdminProvider` فُحصت أيضاً وتشترك نفس الفجوة بعدد أكبر من الدوال — لكن
تُركت عمداً خارج نطاق هذا الإصلاح: منصة إدارية داخلية (ليست بمواجهة
العميل/الفني)، و`loading` بها مُتشابك مع عدد أكبر بكثير من مسارات
القراءة/الكتابة (٣٠+ دالة) بما يرفع مخاطرة إدخال حارس سريع بلا مراجعة
دقيقة لكل مسار على حدة — نفس فلسفة الحذر المتَّبعة بكل هذا الملف
("إعداد جزئي غير قابل للتحقق أسوأ من عدم الإعداد إطلاقاً"، `FIX-CRASHREPORT-IOS-01`).
يستحق مراجعة مستقلة لاحقاً بنفس هذا النمط.

## الحل
حارس داخلي مخصَّص (`bool` خاص منفصل عن `loading`/`sending` العام) لكل
دالة كتابة بالقوائم أعلاه — يُفحَص كأول سطر (`if (_x) return;`)، يُضبَط
`true` فوراً بعده (قبل أي `await`)، ويُعاد لـ`false` بـ`finally`. `loading`/
`submitting`/`sending` العامة (المستخدَمة لتعطيل الزر بصرياً) بقيت كما
هي بلا أي تغيير — الحارس الجديد إضافة مستقلة، لا استبدال.

`WalletProvider`/`SupportProvider` لم يكونا يدعمان حقن API مموَّه للاختبار
أصلاً (بعكس `RequestsProvider`/`ChatProvider`/`AdminProvider`) — أُضيف
معامل `apiOverride` اختياري بنفس نمط `[FIX-TEST-01]` المُعتمَد أصلاً بهذا
المشروع لكليهما، بلا أي تأثير على `app.dart` الذي لا يمرره إطلاقاً.

## الإثبات
السيناريو العدائي المُختبَر مطابق حرفياً لما يحدث فعلياً عند ضغطتين
سريعتين جداً على نفس الزر: استدعاء دالة الكتابة **مرتين متتاليتين بلا
`await` بين الاستدعاءين** (الشكل الدقيق لضغطتين تصلان كلتاهما قبل أن ينفَّذ
إطار الرسم الذي يُعطِّل الزر)، ثم التحقق من وصول نداء API حقيقي **واحد**
فقط للخادم، لا اثنين.

- `test/models/requests_provider_test.dart` — مجموعة جديدة
  `SEC-FIX-DOUBLESUBMIT-01`: 3 اختبارات (`acceptOffer`، `createRequest`،
  `cancelRequest`، كل واحد يمثّل فئة إجراء مختلفة: قرار/إنشاء/إلغاء)، كل
  واحد يتحقق أن الـmock استُدعي مرة واحدة (`.called(1)`) رغم استدعاء دالة
  الـprovider مرتين.
- `test/models/wallet_provider_doublesubmit_test.dart` — ملف جديد،
  اختبار `submitTopup` بنفس النمط.
- `test/models/support_provider_doublesubmit_test.dart` — ملف جديد،
  اختباران (`createTicket`/`sendMessage`) بنفس النمط.

**تحقَّق يدوياً أن كل الاختبارات الجديدة تفشل فعلاً بدون الإصلاح**
(`git stash` مؤقت لملفات الـproviders الثلاثة فقط — `requests_provider.dart`/
`wallet_provider.dart`/`support_provider.dart` — مع إبقاء ملفات الاختبار
الجديدة كما هي): الثلاثة اختبارات بـ`requests_provider_test.dart` فشلت
فعلياً بـ`Expected: <1>, Actual: <2>` (نداء API مرتين فعلاً، بالضبط العطل
الموصوف)، وملفا `wallet_provider_doublesubmit_test.dart`/
`support_provider_doublesubmit_test.dart` فشلا بخطأ ترجمة (`No named
parameter with the name 'apiOverride'`) — دليل إضافي أن القدرة على حقن
Mock لاختبار هذه الفئة لم تكن موجودة أصلاً بالكود القديم. استُعيد الإصلاح
بعدها (`git stash pop`)، ونجاح تام لكل الاختبارات الجديدة.

`flutter analyze`: صفر أخطاء (نفس ١٦ ملاحظة `info` أسلوبية موثَّقة أصلاً
بـ`SEC-FIX-CTXLINT-01`، بلا تغيير). `flutter test --exclude-tags
screenshot` (أمر CI بالضبط): **307 اختبار، نجاح تام، بلا انحدار** (كانت
301 قبل هذه الجولة).

## نطاق متروك عمداً
`AdminProvider` (راجع تفصيل السبب أعلاه بقسم "الاكتشاف") و`AuthProvider`
(`login`/`register`/`verifyOtp`/`forgotPassword`/`resetPassword` تشترك نفس
الفجوة بالضبط — لكن عواقب ضغطة مزدوجة هناك أخف عملياً: الخادم يفرض تفرّد
البريد الإلكتروني عند التسجيل فيرفض المحاولة الثانية برسالة واضحة، وتسجيل
الدخول المزدوج غير ضار جوهرياً/idempotent تقريباً) لم تُلمَسا بهذه الجولة.
نفس الحل (حارس داخلي مخصَّص منفصل عن `loading`) قابل للتطبيق المباشر
عليهما لاحقاً لو ظهر دليل تكرار فعلي أو أُريد إغلاق الفئة كاملة بلا استثناء.

# مراجعة — بقية فئات مسح "يبدو حامياً لكنه ليس كذلك" لم تكشف عن ثغرات إضافية

## الخطورة
لا ينطبق — لا كود تغيّر بهذا المُدخَل بخلاف ما وُثِّق أعلاه، توثيق تأكيدي
فقط لبقية فئات الجولة.

## الفئات المفحوصة والنتيجة
- **`TokenStorage`/تسجيل الخروج** (`lib/core/storage/token_storage.dart`):
  `clearToken()` تستدعي فعلياً `FlutterSecureStorage().delete()` الحقيقية
  (لا علم/متغيّر بالذاكرة فقط) وتُنتظَر (`await`) بأول سطر بـ
  `AuthProvider.logout()`/`deleteAccount()` قبل أي شيء آخر — تحقَّق مباشرة
  أن التخزين الفعلي يُمسَح، لا مجرد حالة داخلية. `getToken()` تُقرَأ من
  التخزين بكل طلب API (`ApiClient`'s `onRequest`) بلا أي تخبئة بالذاكرة —
  لا مسار يُبقي توكناً "شبحياً" فعّالاً بعد `clearToken()`. منطق الشفاء
  الذاتي (`FIX-SECURESTORAGE-01`) يصل بالضبط للحالة النهائية الصحيحة في
  الحالتين (`saveToken` تنجح فعلياً بالمحاولة الثانية بعد `deleteAll`،
  `getToken`/`clearToken` يُعاملان الفشل كـ"لا يوجد توكن" بدل استثناء غير
  معالَج) — لا حالة متبقية خاطئة، مطابق تماماً لما يوثّقه ذلك الإصلاح
  أصلاً (اختبارات موجودة سلفاً بـ`test/core/storage/token_storage_test.dart`).
- **فلترة محتوى رسائل الشات client-side** (بحث عن معالجة `[image]`/
  `[audio]`/`[location]` تُشابه `isSpoofedMediaBody`/`chatViolationReason`
  بالخادم): لا يوجد أي فلتر/تحقق من هذا النوع بجانب العميل إطلاقاً —
  `ChatApi.sendMessage`/`sendLocation` (`lib/features/chat/data/chat_api.dart`)
  يُرسلان أي نص كما هو مباشرة للخادم، و`MessageModel` (`lib/models/message_model.dart`)
  تكتشف البادئات (`isAudio`/`isLocation`/`isImage`) **لغرض العرض فقط** (أي
  ودجت يُبنى للرسالة)، لا كتحقق/حظر لأي شيء. بما أنه لا يوجد كود يَدَّعي
  الحماية أصلاً هنا، لا ينطبق عليه نمط "يبدو حامياً وليس كذلك" — الفرض
  الأمني الوحيد فعلياً بالخادم كما هو موثَّق مسبقاً، بلا أي طبقة عميل
  زائفة توحي بخلاف ذلك.
- **حراسة "قيد الإرسال بالفعل" على أزرار الإرسال**: الفئة الوحيدة التي
  كشفت ثغرة فعلية — راجع `SEC-FIX-DOUBLESUBMIT-01` أعلاه.
- **فحوصات نوع/حجم الملف قبل الرفع** (`image_picker`): لا يوجد أي كود
  بجانب العميل يدّعي التحقق من حجم/نوع/محتوى الملف قبل الرفع (بحث شامل عن
  `fileSize`/`lengthSync`/حدود حجم بالميجابايت — صفر نتائج) — الرفع يمر
  مباشرة لـ`MultipartFile.fromFile` بلا أي بوابة عميل. مثل البند السابق:
  بما أنه لا يوجد كود يَدَّعي هذا الفحص، لا ينطبق نمط "يبدو حامياً وليس
  كذلك" — لا فحص عميل ليُكتشَف كسره أصلاً (الخادم مسؤول كاملاً عن هذا،
  خارج نطاق هذا المستودع).
- **تفعيل/تعطيل عرض إجراء حسب الدور** (`user.isAdmin`/`isTechnician`/
  `isCustomer` بـ`lib/models/user_model.dart`، `RouteGuard`، وكل مواضع
  استخدامها بشاشات الإعدادات/الدعم): مقارنات نصية مباشرة صحيحة
  (`role == 'admin'`/`'technician'`/`'customer'`)، لا تبديل نسخ ولا خطأ
  نسخ-ولصق بين الأدوار الثلاثة بأي موقع فُحص. هذه أصلاً بوابات عرض واجهة
  فقط (UX)، لا حدود صلاحية حقيقية — الخادم يفرض الصلاحيات الفعلية عبر
  middleware مصادقة بمستودع الخادم بغضّ النظر عن حالة الواجهة، بنفس نمط
  موثَّق مسبقاً بهذا الملف لحالات مشابهة.
- **regex/تنقية على نص قبل عرضه أو إرساله للخادم**: البحث الوحيد عن
  `RegExp(` بكل `lib/` أعاد فقط الثلاثة مواقع تحقّق نموذج التسجيل
  (هاتف/رقم وطني) التي تحقَّق منها صاحب المنتج مسبقاً بنفسه (مذكور بوصف
  المهمة) — لا يوجد أي regex/تنقية إضافية تدّعي تعقيم نص قبل عرضه (`BidiText`
  مثلاً أداة اتجاه نص فقط، لا تنقية محتوى — راجع تعليقها المصدري).

لا اكتشاف إضافي بأي من هذه الفئات الستّ — الفئة الوحيدة التي كشفت خللاً
حقيقياً كانت حراسة الضغط المزدوج، مُوثَّقة ومُصلَحة أعلاه.

# [TEST-FIX-NOTIFTAP-01] [TEST-FIX-NOTIFSHOW-01] ترقية `permission_handler`/`flutter_local_notifications` + اختبار حدود Dart الحقيقية لسلسلة الإشعارات

## الخطورة
منخفض — لا ثغرة، ولا خلل سلوكي حالي؛ ترقيتَي حزمتين بلا ثغرة معروفة على
الإصدار المُثبَّت حالياً (فجوة قدم لا خلل نشط)، بالإضافة لاختبارات جديدة
تغلق فجوة تغطية حقيقية اكتُشفت أثناء التحقيق.

## السبب
تدقيق الاعتماديات (`SEC-FIX-DEPAUDIT-01` بالخادم) امتد للعميل: `permission_handler`
(٢ إصدار رئيسي متأخرة) و`flutter_local_notifications` (٣ إصدارات متأخرة).
لا ثغرة معروفة على أي منهما بالإصدار المُثبَّت — الخطر هنا "قدم"، لا خلل
نشط، وتحديداً على محور تغيّر نماذج صلاحيات أندرويد بكل إصدار.

## التحقيق قبل أي ترقية
راجع كل استدعاء فعلي لكلتا الحزمتين بالمشروع مقابل CHANGELOG الرسمي لكل
إصدار رئيسي بينهما (لا افتراضاً):
- **`permission_handler`**: `Permission.microphone`/`.camera`/`.photos`،
  `.status`، `.request()`، `openAppSettings()` — كامل استخدام هذا المشروع —
  بلا أي تغيير كسري عبر 12.0.0/13.0.0. الفرق الوحيد: رفع `compileSdk`
  المطلوب، مُغطّى أصلاً (Flutter 3.47.2 المُثبَّت يضبط `compileSdkVersion=36`
  افتراضياً — تحقَّق مباشرة بمصدر SDK). **لا تعديل كود لزم فعلياً.**
- **`flutter_local_notifications`**: لا استخدام لـ`zonedSchedule`/الجدولة
  إطلاقاً بهذا المشروع (فحص مباشر)، ولا لفئات/أفعال إشعار Darwin، ولا
  استهداف Windows/Web — فكل التغييرات الكسرية بـ21/22 غير ذات صلة.
  التغيير الكسري الوحيد الفعلي: v20.0.0 حوّلت معاملات `initialize()`/`show()`
  الموضعية لمُسمّاة. موقعا الاستدعاء الوحيدان (`_initLocalNotifications`،
  `_showLocalNotificationStatic`) عُدِّلا لـ`settings:`/`id:`/`title:`/`body:`/
  `notificationDetails:` — مطابقة للتوقيع الحقيقي بمصدر الحزمة v22.3.0
  (`platform_flutter_local_notifications.dart`)، لا تخمين من نص CHANGELOG.

## الترقية والتحقق
كل حزمة بالتزام منفصل. لكل التزام: `flutter analyze` نظيف، `flutter test
--exclude-tags screenshot` مطابق تماماً للعدد السابق قبل أي إضافة اختبار
جديدة، **ثم بناء Android حقيقي عبر CI الفعلي** (لا بيئة هذا الوكيل — بلا
Android SDK محلياً هنا) — `.github/workflows/android-build.yml`'s
`flutter build appbundle --release` نجح بشكل مستقل لكل التزام على حدة
(دُفع كل التزام منفرداً، انتُظرت نتيجة CI الفعلية قبل دفع التالي — حتى
يُعزى أي عطل للحزمة الصحيحة بالضبط).

## فجوة تغطية اكتُشفت ومُعالَجة أثناء التحقيق: "الاختبار يمرّ والإشعارات معطَّلة"
لوحظ أثناء المراجعة أن `firebase_background_handler_test.dart` الحالي كان
يثبت فقط "لا يرمي استثناءً" (فشل `show()` نفسه بصمت داخلياً بيئة الاختبار
— `MissingPluginException`/`LateInitializationError`) — لا يثبت إطلاقاً
"استدعاء `show()` يحمل العنوان/النص/الحمولة الصحيحة المُستخرَجة من رسالة
FCM الواردة". اختبار يمر أخضر بهذا الشكل لا يمنع سيناريو "الكود يترجم
الرسالة بشكل خاطئ (حقل مفقود، تبديل عنوان/نص) لكن الاستثناء يُبتلَع فيبدو
كل شيء سليماً".

**الحل**: مجموعة اختبارات جديدة تُسجِّل `AndroidFlutterLocalNotificationsPlugin`
الحقيقية (لا مزيَّفة يدوياً) كمنفِّذ منصّة عبر `FlutterLocalNotificationsPlatform.instance`
+ `debugDefaultTargetPlatformOverride = TargetPlatform.android` (كلاهما
يتحقق منهما `resolvePlatformSpecificImplementation<AndroidFlutterLocalNotificationsPlugin>()`
داخلياً — راجعنا المصدر مباشرة لا خمّنّا)، ثم تحاكي قناة المنصّة الحقيقية
نفسها (`dexterous.com/flutter/local_notifications`، الاسم الحرفي من مصدر
الحزمة) لالتقاط استدعاء `'show'` الفعلي والتحقق من معاملاته — بدل قبول أن
الاستدعاء "لا يرمي" فقط. كذلك `handleNotificationTap` (كانت
`_handleNotificationTap`) رُفعت لـ`@visibleForTesting` بلا underscore —
تغيير صغير مقصود يتيح اختبار الدالة الحقيقية مباشرة (لا نسخة مُعاد كتابتها
لمنطقها بملف الاختبار كما كان الحال سابقاً بـ`notification_deeplink_test.dart`).

## الإثبات
- `test/widgets/firebase_background_handler_test.dart`: مجموعة جديدة —
  رسالة FCM حقيقية (بيانات فقط) → `show()` تستقبل العنوان/النص/الحمولة
  المطابقة تماماً؛ وحالة احتياطية (رسالة بلا title/body) → القيم
  الافتراضية الصحيحة. **تحقَّق يدوياً بدون التعديل**: فشل حقيقي —
  `debugAssertAllFoundationVarsUnset` يرمي إن أُعيد `debugDefaultTargetPlatformOverride`
  بـ`tearDown` منفصل بدل داخل الاختبار نفسه (تفصيل مكتشَف أثناء التنفيذ:
  فحص الثوابت يُنفَّذ **قبل** أي `tearDown` خارجي مسجَّل، لا بعده) — أُصلح
  بنمط try/finally داخل كل اختبار مباشرة.
- `test/widgets/notification_deeplink_test.dart`: الاختبار القائم عُدِّل
  ليستدعي `handleNotificationTap` الحقيقية بدل تعيين `pendingDeepLink.value`
  يدوياً؛ اختباران جديدان: حمولة بلا `type`/`type` فارغ لا تنشر شيئاً، وحمولة
  صحيحة تنشر نسخة مستقلة (`Map.from`، لا مرجعاً للأصل). **تحقَّق يدوياً أن
  الاختبارات الثلاثة تفشل فعلاً بدون رفع الرؤية** (`git stash` مؤقت لملف
  المصدر فقط): فشل تجميع كامل (`handleNotificationTap` غير موجودة بلا
  التعديل) — إثبات ملموس أن رفع الرؤية ضروري فعلياً، لا زخرفة.

المجموعة الكاملة: **311/311** (كانت 307، +4 اختبارات جديدة) — صفر تراجع.
`flutter analyze`: نظيف، نفس ١٦ ملاحظة `info` موجودة مسبقاً.

## حدود هذه الاختبارات — ما لا تثبته، بالنص الصريح
هذه الاختبارات تثبت أن **جانب Dart من الحدّ** يعمل صحيحاً: عند استلام رسالة
FCM، الكود الصحيح يُستدعى بالمعاملات الصحيحة (`show()`)، وعند الضغط على
إشعار، الكود الصحيح يُستدعى بالحمولة الصحيحة (`handleNotificationTap`). هي
**لا تثبت** أن الإشعار وصل فعلياً من خوادم Google، أو أن Google Play
Services على الجهاز عالج الرسالة، أو أن نظام أندرويد نفسه عرض الإشعار على
الشاشة فعلياً، أو أن الضغط الحقيقي على الشاشة يستدعي فعلياً
`onDidReceiveNotificationResponse` (يتطلَّب `_initLocalNotifications()` كاملة
عبر `init()`، وهذه تتطلَّب `Firebase.initializeApp()` حقيقية — غير مُتاحة
ببيئة `flutter_test`، خارج نطاق هذا التغيير عمداً بسبب التكلفة غير
المتناسبة). السلسلة الكاملة (خوادم Google → Play Services → كود
Java/Kotlin أصلي بالحزمة → نظام إشعارات أندرويد) تمر بطبقات خارج أي شيء
يختبره `flutter test` بمعزل عن جهاز حقيقي — هذا بالضبط سبب طلب فحص يدوي
حقيقي على جهاز فعلي قبل الدمج (راجع محادثة هذا التغيير)، ولا بديل برمجي
كامل يغني عنه.

# [BUG-FIX-DEEPLINKRACE-01] `onAuthenticated` كانت تمسح `pendingDeepLink` قبل أن يستهلكه أي Layout — تراجع أدخلته `SEC-FIX-DEEPLINKCLEAR-01`

## الخطورة
متوسط — لا تسريب بيانات ولا مشكلة أمنية، لكن انكسار وظيفي مضمون ١٠٠٪ من
الوقت (لا حافة نادرة): أي مستخدم يضغط إشعاراً يفتح التطبيق من إغلاق تام
(cold start) وله جلسة محفوظة، يفقد هدف التنقّل بصمت في كل مرة. الميزة
التي [FIX-DEEPLINK-01] بناها خصيصاً لهذه الحالة كانت معطَّلة فعلياً منذ
إدخال [SEC-FIX-DEEPLINKCLEAR-01] (جولة سابعة) دون أن يلتقطها أي اختبار،
لأن ذلك الإصلاح نفسه وثَّق صراحةً "لا اختبار مخصَّص" لتغييره.

## الاكتشاف
جولة تدقيق عاشرة (تدقيق تفاعل بين الترتيب الزمني لتهيئة `main.dart`
ودورة حياة `AuthProvider`، لا قراءة مسارات/شاشات مباشرة). الترتيب الفعلي:
`main.dart` يستدعي `FirebaseNotificationService.init()` (يملأ
`pendingDeepLink` من `getInitialMessage()` عند إقلاع بارد بضغطة إشعار)
**قبل** `runApp()` — أي قبل بناء أي شجرة ودجت إطلاقاً. `AuthProvider.onAuthenticated`
(المُوصَّلة بـ`_SocketBootstrapperState.didChangeDependencies`) تُطلَق أثناء
استعادة الجلسة، وهي بدورها **قبل** بناء أي `Layout` بحسب الدور (عميل/فني/
أدمن) — أول جهة قادرة فعلياً على استهلاك `pendingDeepLink` (عبر `addListener`
أو `postFrameCallback` الخاصين بها بـ`initState`). `SEC-FIX-DEEPLINKCLEAR-01`
أضافت `FirebaseNotificationService.pendingDeepLink.value = null;` داخل
`onAuthenticated` نفسها — أي أنها كانت تُفرِغ القيمة قبل أن يصل أي مستهلك
حقيقي لها بأي مسار إقلاع بارد من ضغطة إشعار، بصرف النظر عن أي حالة سباق؛
هذا الترتيب ثابت دائماً بحكم بنية `main.dart` نفسها.

سيناريو التسريب الأصلي الذي صُمِّم مسح `onAuthenticated` لأجله (حساب أ يستلم
إشعاراً، يُبقي التطبيق بالخلفية دون ضغطه، يسجّل خروجه؛ حساب ب يدخل على نفس
الجهاز فيُستهلَك هدف تنقّل حساب أ القديم) يبقى مغطّى بالكامل عبر
`onLoggedOut` (تُطلَق من `logout()`/`deleteAccount()`/`handleUnauthorized()`
الثلاثة، وتُنفَّذ فوراً عند لحظة الخروج الفعلية — لا حاجة لمسح مكرَّر جهة
`onAuthenticated`).

## الحل
1. حُذِف `FirebaseNotificationService.pendingDeepLink.value = null;` من
   داخل `onAuthenticated` فقط (`lib/app.dart`) — بقي كما هو داخل
   `onLoggedOut`، فسيناريو `SEC-FIX-DEEPLINKCLEAR-01` الأصلي يبقى محلولاً
   بالكامل.
2. `onAuthenticated`/`onLoggedOut` كانا إغلاقين (closures) مُضمَّنين مباشرة
   داخل `_SocketBootstrapperState.didChangeDependencies` — غير قابلين
   للاستدعاء أو الاختبار بمعزل عن بناء شجرة الودجت الكاملة، وهو بالضبط
   سبب غياب أي اختبار عند إدخال الخلل أصلاً. اسْتُخرِجا لدالة علوية مستقلة
   `bindAuthLifecycleCallbacks({...})` بمعاملات `Provider` صريحة — بلا أي
   تغيير سلوكي عن الإغلاق السابق، فقط قابلية استدعاء مباشرة من اختبار.

## الإثبات
`test/widgets/auth_lifecycle_deeplink_test.dart` (جديد) — يستدعي
`bindAuthLifecycleCallbacks` الحقيقية بمزوّدات وهمية (`Mock`/`apiOverride`،
بلا أي اتصال شبكة أو Firebase حقيقي):
1. يضبط `pendingDeepLink` كما يفعل `main.dart` فعلياً قبل استدعاء
   `onAuthenticated`، ثم يثبت أن القيمة تبقى موجودة بعدها — أي متاحة لأي
   `Layout` يُبنى لاحقاً.
2. يثبت أن `onLoggedOut` وحدها من تمسح `pendingDeepLink` (تغطية مستمرة
   لسيناريو `SEC-FIX-DEEPLINKCLEAR-01` الأصلي).

**تحقَّق يدوياً fail-before/pass-after** (`git stash` مؤقت لـ`lib/app.dart`
فقط، الاختبار الجديد يبقى بالشجرة): بدون التعديل، فشل تجميع كامل
(`bindAuthLifecycleCallbacks` غير موجودة — الدالة نفسها جزء من الحل).
تحقُّق إضافي أعمق: أُعيد حقن سطر المسح الخاطئ داخل الدالة المُستخرَجة
الجديدة نفسها (لا الإغلاق القديم) للتأكد من أن الاختبار يفشل بفشل تأكيد
حقيقي (`expected {...}, actual: null`) لا مجرد خطأ تجميع — نجح، ثم أُزيل
السطر مجدداً. الاختبار الثاني (`onLoggedOut`) يمر بالحالتين (لم يتغيّر).

المجموعة الكاملة: **313 اختباراً ناجحاً** (زيادة اختبارين عن الأساس)، لا
تراجع في أي اختبار قائم. الفشل الوحيد المتبقي بهذه البيئة
(`l10n_screenshot_capture_test.dart`، `ARABIC_FONT_PATHS` فارغ) بيئي بحت
(يتطلَّب خطوة تثبيت خطوط من CI غير متاحة بهذا الـsandbox) — غير متعلق
بهذا التغيير، وموجود مسبقاً بمعزل عنه.

## نطاق متروك عمداً
لا اختبار لسيناريو iOS المكافئ (`getInitialMessage()` عبر مسار مختلف قليلاً
على iOS) — خارج بيئة `flutter test` أصلاً (يتطلَّب جهاز/محاكي iOS حقيقي)،
ونفس القيد المُوثَّق بـ`TEST-FIX-NOTIFTAP-01`.

# [FIX-SOCKETREBIND-01] `SocketProvider.connect()` لا تُعيد تسجيل المستمعين إن استُدعيت مرتين — بلا اتصال لحظي حي بعد استئناف التطبيق من انقطاع طويل

## الخطورة
خطير — لا فقدان بيانات دائم (كل شيء يعود بإعادة تشغيل كاملة للتطبيق)، لكن
المستخدم يبقى بلا شات/إشعارات/تحديثات طلبات لحظية بصمت تام (لا رسالة خطأ،
لا مؤشر انقطاع — `SocketProvider.connected` تبقى `false` للأبد بهذه الحالة)
حتى يسجّل خروجه ودخوله من جديد، أو يُعيد تشغيل التطبيق بالكامل. يتطلَّب
انقطاع شبكة أطول من نافذة إعادة المحاولة الداخلية لـsocket.io (~6 دقائق،
راجع `BW-FIX-01`) ثم عودة التطبيق للمقدمة — سيناريو واقعي شائع (نوم الجهاز،
نفق مترو، تبديل شبكة طويل) لا حافة نظرية.

## الاكتشاف
جولة تدقيق عاشرة (تدقيق تفاعل بين مسارين كل منهما صحيح بمعزل: منطق إعادة
اتصال socket.io الداخلي، ومسار استئناف التطبيق بـ`app.dart`). التسلسل:

1. `SocketService.connect()` (`socket_service.dart:19-21`) تستدعي
   `disconnect()` (تُفرِّغ وتُعدِم كائن `io.Socket` الحالي) ثم تُنشئ كائن
   `io.Socket` **جديد تماماً** — في **كل** استدعاء، بصرف النظر عن وجود
   اتصال سابق نشط أم لا (`enableForceNew()` أصلاً موثَّقة بـ`FIX-SOCKET-01`
   لسبب مشابه: توكن جديد لكل اتصال).
2. `SocketProvider._bindSocketListeners()` (`socket_provider.dart`) محروسة
   بـ`_listenersBound` — تُعاد ضبطها لـ`false` فقط داخل `reconnect()` (تبديل
   مستخدم) و`disconnect()` (تسجيل خروج)، **لا** داخل `connect()` نفسها.
3. `_SocketBootstrapperState.didChangeAppLifecycleState` (`app.dart`) عند
   استئناف التطبيق من الخلفية، إن كان `!socketProvider.connected`، يستدعي
   `socketProvider.connect()` **مباشرة** — لا `reconnect()`.

النتيجة: بعد انقطاع شبكة يستنفد محاولات إعادة الاتصال الداخلية لـsocket.io
(فـ`connected` تصير `false` عبر حدث `disconnect` المُسجَّل أصلاً)، استئناف
التطبيق يستدعي `connect()`. `socketService.connect()` تُنشئ كائن `io.Socket`
جديداً بلا أي مستمع عليه. لكن `_listenersBound` لا تزال `true` من الاتصال
الأول، فـ`_bindSocketListeners()` تُرجِع فوراً بلا تسجيل شيء على الكائن
الجديد — يشمل حتى مستمع حدث `connect` نفسه، الذي يحدّث `SocketProvider.connected`.
فالسوكت يتصل فعلياً من منظور socket.io، لكن `connected` تبقى `false` للأبد
(لا شيء يُحدِّثها)، وكل التحديثات اللحظية (شات، طلبات، رصيد، دعم) ميتة بصمت.

## الحل
`SocketProvider.connect()` تُعيد ضبط `_listenersBound = false` قبل استدعاء
`_bindSocketListeners()` — بما أن كل استدعاء لـ`socketService.connect()`
ينتج كائن سوكت جديداً دائماً (لا استثناء)، فكل استدعاء لـ`connect()` يلزمه
تسجيل مستمعين جدد، بصرف النظر عن أي استدعاء سابق. `reconnect()` تبقي إعادة
ضبطها الصريحة الخاصة بها (زائدة الآن لكن غير ضارة — لا داعي لتبسيطها).

## الاختبار المُضلِّل الذي كان يوثِّق الخلل كسلوك صحيح
`test/models/socket_provider_test.dart` كان يحمل اختباراً باسم "connect()
مرتين متتاليتين: المستمعون يُسجَّلون مرة واحدة فقط (_listenersBound)"
يتحقق صراحة من `.called(1)` بعد استدعاءين — أي يُثبت الخلل نفسه كسلوك
مقصود. عُدِّل الاختبار (الاسم والجسم) ليعكس السلوك الصحيح: كل استدعاء
`connect()` يُعيد تسجيل المجموعة الكاملة، ويثبت إضافياً أن المستمع المُسجَّل
بالمرة الثانية فعلياً حي (يُستدعى ويحدِّث `connected`) — لا فقط أن `.on()`
استُدعيت مجدداً.

## الإثبات
**تحقَّق يدوياً fail-before/pass-after** (`git stash` مؤقت لـ`socket_provider.dart`
فقط، الاختبار المُعدَّل يبقى بالشجرة): بدون الإصلاح، فشل حقيقي —
`capturedCallbackFor(SocketEvents.connect)` بعد الاستدعاء الثاني يرمي
`StateError: لم يُسجَّل أي مستمع للحدث: connect` (نفس ما يحدث فعلياً
بالتطبيق: صفر مستمعين على الكائن الجديد). بعد الإصلاح: **313 اختباراً
ناجحاً**، لا تراجع بأي اختبار قائم.

## نطاق متروك عمداً
لا اختبار end-to-end حقيقي عبر `socket_io_client` فعلي (خادم Socket.IO
حقيقي أو وهمي على مستوى بروتوكول WebSocket) — الاختبار الحالي (كبقية ملف
`socket_provider_test.dart`) يعمل على مستوى `SocketService` المُقلَّد
(Mock)، لا يثبت تفاعل حزمة `socket_io_client` نفسها مع خادم حقيقي.

# [SEC-FIX-MEPWSOCKET-CLIENT-01] `ChangePasswordScreen` لا تُعيد اتصال السوكت بعد نجاح تغيير كلمة السر — الخادم يقطعه الآن فوراً

## الخطورة
متوسط — لا مشكلة أمنية بحد ذاتها (نتيجة مباشرة لإصلاح أمني صحيح بالمستودع
المقابل)، لكن انكسار وظيفي مضمون بعد كل تغيير كلمة سر ناجح: شات/إشعارات
لحظية ميتة بصمت على الجهاز الحالي نفسه حتى استئناف التطبيق التالي من
الخلفية أو تسجيل خروج/دخول جديد.

## الاكتشاف
جولة تدقيق عاشرة، أثناء تنفيذ إصلاح فك أمني على المستودع المقابل
(`SEC-FIX-MEPWSOCKET-01`، `sallehly`): `POST /me/password` أصبح يستدعي
`io.in(user-${id}).disconnectSockets(true)` فور نجاح تغيير كلمة السر —
بنفس نمط `/auth/reset-password`/مسارات الأدمن. **الفرق الجوهري**: تلك
المسارات الأخرى تقطع جلسة *جهاز آخر* (نسخة توكن مسروقة) أو *حساب آخر*
(إجراء أدمن) — لا الجهاز الذي نفّذ الطلب نفسه، والذي يستمر باستخدام
التطبيق مباشرة بعد ذلك. `/me/password` مختلف: يُنفَّذ من نفس الجهاز الذي
يملك عادة سوكتاً حياً بنفس الحساب، فيُقطع أيضاً. `ChangePasswordScreen`
(`lib/features/settings/screens/change_password_screen.dart`) لم تكن
تستدعي أي إعادة اتصال — `authApi.changePassword()` نفسها تتجاهل جسم
الاستجابة (`{ok, token}`) بالكامل، معتمدة ضمنياً على أن `ApiClient`'s
معترِض عام (`onResponse`) يحفظ التوكن الجديد تلقائياً من ترويسة
`Set-Cookie` (آلية موجودة أصلاً لكل استجابة، لا خاصة بهذا المسار) —
`tokenStorage` يتحدَّث بالفعل، لكن لا شيء يستخدمه لإعادة اتصال السوكت.

## الحل
`ChangePasswordScreen.submit()` تستدعي `context.read<SocketProvider>().reconnect()`
فور نجاح `auth.changePassword(...)` (بلا `await` — لا داعي لتأخير التنقّل
بانتظار اتصال السوكت، نفس فلسفة `didChangeAppLifecycleState` بـ`app.dart`).
`reconnect()` تقرأ `tokenStorage` من جديد (التوكن الجديد المحفوظ تلقائياً
كما هو موضَّح أعلاه) وتُعيد تسجيل كل المستمعين على الكائن الجديد (`FIX-SOCKETREBIND-01`
يضمن هذا يعمل صحيحاً حتى لو استُدعيت `connect()` أكثر من مرة).

## الإثبات
`test/widgets/change_password_socket_reconnect_test.dart` (جديد) — يملأ
نموذج `ChangePasswordScreen` الحقيقي، يضغط زر الحفظ، يثبت أن
`SocketProvider` الحقيقية (بمزوّدات `SocketService`/`TokenStorage` وهمية)
تستدعي `disconnect()` ثم `connect(token: 'fresh-token-after-change')`
وتُعيد تسجيل مستمع حدث `connect`. **تحقَّق يدوياً fail-before/pass-after**
(`git stash` مؤقت لـ`change_password_screen.dart` فقط): بدون التعديل، فشل
حقيقي — "No matching calls (actually, no calls at all)" (السوكت لا يُعاد
اتصاله إطلاقاً). المجموعة الكاملة: **314 اختباراً ناجحاً** (زيادة اختبار
واحد)، لا تراجع. الفشل البيئي الوحيد المتبقي (`l10n_screenshot_capture_test.dart`،
خطوط CI غير متاحة بهذا الـsandbox) غير متعلق بهذا التغيير.

## نطاق متروك عمداً
لا تحقّق من أن `reconnect()` تنجح فعلياً بتوكن السيرفر الحقيقي (يتطلَّب
خادم حقيقي/اختبار تكامل — خارج نطاق `flutter test` المعزول). الثقة هنا
مبنية على أن `SEC-FIX-MEPWSOCKET-01` (الخادم) يُصدر توكناً يحمل
`token_version` الجديد فعلياً (مُثبَت هناك بالفعل)، وأن `ApiClient`'s حفظ
التوكن من `Set-Cookie` آلية عامة مُختبَرة ضمنياً بمسارات أخرى.

# [SEC-FIX-ADMINDOUBLESUBMIT-01] `AdminProvider.actionLoading` يُكتَب بـ17 دالة كتابة، يُقرَأ كحارس بصفر منها — على الطريقتين اللتين تحرِّكان أموالاً فعلياً

## الخطورة
متوسط — نفس فئة وحجم `SEC-FIX-DOUBLESUBMIT-01` بالضبط (نافذة استغلال ضيقة
جداً، أسرع من إطار رسم واحد؛ ~16ms)، لكن العواقب هنا أوضح مالياً:
`reviewTopup` تعتمد شحن رصيد (يزيد رصيد فني فعلياً)، `adjustUserBalance`
تُعدِّل رصيد مستخدم مباشرة — ضغطتان سريعتان على نفس الزر (تلامس بشري
غير مقصود، أو اتصال بطيء يُغري الأدمن بالضغط ثانية) كانتا تُرسلان العملية
مرتين حقيقيتين للخادم.

## الاكتشاف
جولة تدقيق عاشرة، فحص مباشر لكل الـ17 دالة كتابة بـ`AdminProvider`
(`lib/features/admin/provider/admin_provider.dart`) التي تكتب
`actionLoading = true/false`: **لا دالة واحدة منها تقرأ `actionLoading`
كحارس قبل البدء** — بالضبط فئة `SEC-FIX-DOUBLESUBMIT-01` (`RequestsProvider`/
`WalletProvider`/`SupportProvider`، جولة سابقة) التي فاتت `AdminProvider`
حينها. تعطيل الزر بالواجهة (`actionLoading ? null : onPressed`) وحده لا
يمنع نداءين حقيقيين متتاليين وصلا بلا `await` بينهما، لأن
`notifyListeners()` لا يُعيد رسم الودجت بنفس السطر — يُجدوِل ذلك للإطار
التالي فقط.

**لماذا حارسان فقط، لا 17**: نفس حجة `SEC-FIX-DOUBLESUBMIT-01` بالضبط —
`actionLoading` مشترك بكل الـ17 دالة معاً، فحارس ساذج عليه مباشرة
(`if (actionLoading) return;`) كان سيمنع بصمت إجراءً أدمن حقيقياً لو كان
إجراء *آخر* مختلف قيد التنفيذ صدفة بنفس اللحظة (مثال: تعديل رصيد مستخدم
بينما مراجعة شحن منفصلة لا تزال معلَّقة). الطلب من المستخدم (محادثة هذا
التغيير) كان مقصوراً صراحة على الطريقتين اللتين تحرِّكان أموالاً فعلياً —
`reviewTopup`/`adjustUserBalance` — لا كل الـ17.

## الحل
حارسان مخصَّصان منفصلان (`_reviewingTopup`/`_adjustingBalance`)، بنفس
نمط `_creatingRequest`/`_sendingOffer`/... بـ`RequestsProvider` حرفياً:
`if (_flag) return; _flag = true;` في أول سطر بجسم الدالة، قبل حتى
`actionLoading = true`، تُصفَّر بـ`finally` مع `actionLoading`.

## الإثبات
`test/models/admin_provider_doublesubmit_test.dart` (جديد) — بنفس نمط
`wallet_provider_doublesubmit_test.dart` تماماً: استدعاءان متزامنان (بلا
`await` بينهما) لكل من `reviewTopup`/`adjustUserBalance`، يثبت أن طلباً
حقيقياً واحداً فقط يصل للـAPI المُقلَّد بكل حالة. **تحقَّق يدوياً
fail-before/pass-after** (`git stash` مؤقت لـ`admin_provider.dart` فقط):
بدون الإصلاح، فشل حقيقي — `Expected: <1>, Actual: <2>` بكلا الاختبارين
(نداءان حقيقيان فعلاً، لا واحد كما يوحي مظهر الزر "المعطَّل"). المجموعة
الكاملة: **316 اختباراً ناجحاً** (زيادة اختبارين)، لا تراجع.

## نطاق متروك عمداً
الـ15 دالة كتابة الأخرى بـ`AdminProvider` (`toggleUser`، `changeUserRole`،
`verifyTechnician`، `updateSupportStatus`، `toggleService`، `updateService`،
`createService`، `deleteService`، `deletePackage`، `createPackage`،
`cancelRequest`، `changeRequestStatus`، `updateUserProfile`، `deleteUser`،
`updatePackage`) تبقى بلا حارس مخصَّص — نفس ثغرة الضغطة المزدوجة قائمة
بها (عمليات مكرَّرة عند حدوثها: توثيق فني مزدوج، تعديل باقة مزدوج...) لكن
لا تحرِّك أموالاً مباشرة مثل الاثنتين أعلاه؛ على قائمة المؤجَّل بخطورة
منخفض–متوسط (نفس تصنيف نظرائها بـ`RequestsProvider` قبل
`SEC-FIX-DOUBLESUBMIT-01`، لو أُريد توسيع التغطية لاحقاً بجولة مخصَّصة).

# [DEFERRED-AUDIT-10] قائمة مؤجَّلة — نتائج جولة التدقيق العاشرة غير المُصلَحة عمداً

الجولة العاشرة (تقنية جديدة عمداً — لا قراءة شاشات/مزوّدات متسلسلة كالجولات
التسع السابقة، بل محور: تفاعل تبعيات مُحدَّثة، سلوك بحجم بيانات حقيقي/بعد
أشهر، انحراف الإعداد بين البيئات، فجوة بين ما تثبته الاختبارات وما تدّعيه،
تفاعل مكوِّنين كل منهما صحيح بمعزل، وما قد يكسره مهندس جديد بأسبوعه الأول)
قيّمت المستودع **7/10**. من بين ما وُجد، ثلاثة بنود عالية استحقّت إصلاحاً
فورياً بقرار صاحب المنتج فوراً بهذه الجلسة (اثنان منها تراجعان حقيقيان
أدخلتهما هذه الهندسة نفسها بجولات سابقة) — راجع `[BUG-FIX-DEEPLINKRACE-01]`،
`[FIX-SOCKETREBIND-01]`، `[SEC-FIX-ADMINDOUBLESUBMIT-01]` أعلاه (الأخير جزئي
— طريقتان فقط من 17). **كل ما تبقى هنا مؤجَّل عمداً بقرار صريح من صاحب
المنتج ("everything else goes on the deferred list with severity") — لم
يُصلَح، ولن يُصلَح بهذه الجلسة.**

كل بند إما مثبَت مباشرة بقراءة الكود (مُعلَّم "مؤكَّد بقراءة المصدر")، أو
منقول كما ورده وكيل التقييم بمنهجية تنفيذ فعلي (لا تخمين) لكن بلا جهاز iOS
حقيقي للتحقق النهائي. القرار الصريح بعد هذه الجولة: تقنية "قراءة الكود
المتسلسلة" اعتُبرت مستنفَدة لهذا المستودع — الجولة التالية المتفَق عليها
هي Maestro E2E (المجموعة موجودة أصلاً بهذا المستودع، كانت ستلتقط تراجع
الرابط العميق H1 مباشرة) لا مزيد من قراءة المصدر.

## عالٍ — خاص بـiOS فقط، مؤكَّد بقراءة المصدر (بلا جهاز iOS حقيقي للتحقق النهائي)
### لا `ios/Podfile` إطلاقاً — أذونات الميكروفون/الكاميرا تُرفَض دائماً على iOS
لا يوجد `ios/Podfile` بالمستودع إطلاقاً. وحدة `permission_handler` تعتمد
على ماكرو Objective-C يُقرَأ من `Podfile` لتفعيل كل فئة إذن على حدة —
بغيابه، الماكرو الافتراضي (مُتحقَّق بقراءة رأس الحزمة نفسها مباشرة) يساوي
`0` لكل الأذونات. النتيجة: أي فحص إذن ميكروفون/كاميرا على iOS يرجع "مرفوض"
دائماً بصرف النظر عن اختيار المستخدم الفعلي بنظام التشغيل — الرسائل
الصوتية بالشات معطَّلة بشكل دائم على iOS. **ليست ناتجة عن ترقية التبعيات
الأخيرة** (خلل سابق للوجود)، ولم تُكتشَف عملياً لأن iOS ليس هدف نشر موقَّع
بعد بهذا المشروع. **الحل المقترح لاحقاً:** إنشاء `ios/Podfile` وتفعيل
ماكروهات الأذونات المطلوبة فعلياً (ميكروفون، كاميرا) قبل أي نشر iOS حقيقي.

## متوسط
### إشعارات FCM بالمقدمة لا تُكتَم للمحادثة المفتوحة فعلياً على الشاشة
مسار السوكت الحي يكتم صوت/اهتزاز إشعار رسالة تخص محادثة العميل يشاهدها
فعلياً حالياً (تحقَّق بالجولات السابقة) — مسار FCM الموازي (يُستقبَل حتى
بينما التطبيق بالمقدمة) لا يطبِّق نفس منطق الكتم. مستخدم يشاهد شاتاً حياً
قد يسمع صوت/اهتزاز إشعار لرسالة تظهر أمامه فعلياً بنفس اللحظة.

### لا تصفّح (Pagination) لسجل رسائل الشات
كل رسالة جديدة تُعيد تحميل/تحليل المحادثة الكاملة بدل جلب الرسائل الجديدة
فقط. لا يظهر كخلل وظيفي بحجم بيانات اليوم، لكن مسار نمو واضح مع تراكم
رسائل محادثات طويلة العمر (بالضبط فئة "لا يظهر إلا بحجم بيانات حقيقي/بعد
أشهر" التي استهدفتها هذه الجولة تحديداً).

### `flutter run` بلا `--dart-define` يضرب واجهة الإنتاج الحية مباشرة، وملاذ التطوير المحلي الموثَّق مكسور
بلا تمرير `--dart-define` صريح، القيمة الافتراضية لعنوان الـAPI تشير
لواجهة الإنتاج الحية — أي مهندس جديد يشغّل `flutter run` كما هو موثَّق
بالـREADME بلا قراءة كل تفصيل، يكتب بيانات حقيقية على الإنتاج بأول تشغيلة
اختبار محلية. **الأسوأ**: البديل الموثَّق لتطوير محلي (`http://10.0.2.2:3000`)
لا يعمل أصلاً — لا شيء يُفعِّل حركة `cleartext` (HTTP غير مشفَّر) على مستوى
Android SDK المستهدف، فحتى مهندس اتّبع التوثيق بدقة يصطدم بفشل اتصال بلا
تفسير واضح. بالضبط فئة "ما سيكسره مهندس جديد بأسبوعه الأول بلا أن يدرك".

## منخفض/تافه — فجوة "الاختبار يمر لكن لا يثبت ما يدّعيه"
- ملفان اختباريان آخران (من 50 إجمالاً بالمستودع) يختبران نسخة معاد كتابتها
  يدوياً من منطق الودجت الحقيقي بدل الودجت نفسه — نفس فئة الفجوة التي
  عولجت صراحة بجولات سابقة (`TEST-FIX-NOTIFTAP-01` وغيره) لكن لم تُغلَق
  بكل الملفات.
- معرِّفات الإشعارات المحلية تتصادم إن وصل أكثر من إشعار خلال نفس الثانية
  (توليد المعرِّف يعتمد على الطابع الزمني بدقة ثانية).
- إصدار Flutter SDK بملف CI غير مثبَّت (عائم) — بناء CI قد يستخدم إصداراً
  مختلفاً عن المطوِّر المحلي بلا أي تنبيه.
- تجاوز معرِّف إشعار عام 2038 (تجاوز `int32` كلاسيكي) — يُبتلَع بصمت عبر
  try/catch موجود مسبقاً، لا يظهر كخطأ. غير عاجل بحكم الأفق الزمني، مذكور
  للتوثيق فقط.

## تفاعل التبعيات الحديثة — فُحص، سليم بلا أي فعل مطلوب (مع تصحيح توثيقي بسيط)
لا تعارض إصدارات بين `firebase_messaging`/`image_picker`/`record`/إلخ.
تصحيح واحد على عمل سابق بهذه الهندسة نفسها: التزام سابق برَّر رفع
`compileSdk` إلى 36 بحجة "Flutter 3.47.2 يغطي هذا أصلاً" — لكن
`permission_handler_android` نفسها تطلب فعلياً `compileSdk 37` (كان
التحقق وقتها راجع ملفاً خاطئاً). لا أثر عملي حالياً (بناء CI الحقيقي نجح
رغم ذلك)، فقط تبرير التزام سابق غير دقيق — يستحق تصحيحاً توثيقياً لا
تعديل كود، ومذكور هنا فقط لاكتمال السجل.

# [SEC-FIX-ADMINDOUBLESUBMIT-02] كل الـ17 دالة كتابة بـAdminProvider تمرّ الآن عبر حارس مشترك واحد — لا حارسين مخصَّصين + 15 دالة بلا حماية

## الخطورة
متوسط — نفس فئة `SEC-FIX-ADMINDOUBLESUBMIT-01` (نافذة استغلال ضيقة جداً)،
لكن هذا التغيير يعالج مشكلة أوسع: **البنية نفسها**، لا فقط التغطية. ترك
حارسين مخصَّصين (`_reviewingTopup`/`_adjustingBalance`) بجانب 15 دالة بلا
أي حارس هو بالضبط نفس فئة "بعض الوحدات محمية، والباقي يبدو محمياً لكنه
ليس كذلك، بلا طريقة مرئية للتفريق بينها" — نمطان مختلفان بنفس الملف يدعوان
أي قارئ لافتراض أن `actionLoading` يحرس كل شيء بالتساوي.

## الاكتشاف
قرار صاحب المنتج مباشرة، بعد `SEC-FIX-ADMINDOUBLESUBMIT-01`: بدل توسعة
النمط القديم (حارس مخصَّص بكل دالة، 15 نسخة إضافية من نفس الشفرة المكرَّرة)
أو تركه جزئياً، طُلب أحد أمرين — تعميم الحماية على الكل، أو جعل الفرق بين
المحمي وغير المحمي واضحاً بنيوياً لا بالقراءة الدقيقة فقط. الخيار المُختار
(B): آلية واحدة تجعل الحماية *إجبارية بالبنية*، لا انضباطاً يدوياً متكرراً.

## الحل
دالة `_runGuarded(String actionKey, Future<void> Function() action)` جديدة
تحرس بمفتاح فريد لكل إجراء (`Set<String> _inFlightActions`، لا حارس منفصل
بكل دالة) — إجراءان *مختلفان* يعملان معاً بشكل مشروع (نفس فلسفة
`SEC-FIX-DOUBLESUBMIT-01` الأصلية)، لكن نفس الإجراء مرتين متتاليتين بلا
`await` بينهما يُرفَض بصمت بالثانية. كل الـ17 دالة كتابة (تشمل الطريقتين
المُصلَحتين سابقاً بحارسين مخصَّصين — `reviewTopup`/`adjustUserBalance`،
اللذان أُزيل حارساهما المنفصلان ودُمِجا بنفس الآلية) أصبحت الآن:
```dart
Future<void> toggleUser(int id, {String? reason}) {
  return _runGuarded('toggleUser', () async { ... نفس المنطق الأصلي حرفياً ... });
}
```
بلا أي تغيير بمنطق كل دالة الداخلي (نفس catch/rethrow أو غيابه، نفس رسائل
الخطأ، نفس استدعاءات API) — فقط استبدال تكرار `actionLoading = true/false`
+ `notifyListeners()` اليدوي بالحارس المشترك. **تحسين إضافي عرضي غير
مطلوب لكنه ضروري لسلامة السلوك**: `actionLoading` أصبح يُشتَق من
`_inFlightActions.isNotEmpty` بدل `false` حرفية بكل `finally` — الكود
القديم (حتى بالحارسين المخصَّصين) كان يجعل `actionLoading` يعود `false`
بمجرد انتهاء أي إجراء واحد، حتى لو كان إجراء *آخر* لا يزال قيد التنفيذ
فعلياً (خلل وميض كامن كان موجوداً أصلاً، لا شيء أدخله هذا التغيير — يُصلَح
هنا كأثر جانبي مجاني للتصميم الجديد).

## الإثبات
`test/models/admin_provider_guard_test.dart` (جديد) — أربع مجموعات:
1. **سلامة الحارس عند استثناء**: `deleteUser`/`toggleService` (أحدهما
   بـcatch/rethrow، الآخر بلا catch إطلاقاً — كلا النمطين الموجودين
   بالملف) تُستدعى بمحاكاة API ترمي بالمحاولة الأولى — يُثبَت أن
   `actionLoading` يعود `false` رغم الاستثناء، **والأهم**: أن استدعاءً
   ثانياً لنفس الدالة يصل فعلياً لـAPI المُقلَّد (لو كان `_inFlightActions`
   لم يُنظَّف بـ`finally` رغم الاستثناء، هذا الاستدعاء الثاني كان سيُرفَض
   بصمت بلا أي نداء API).
2. **اكتمال التغطية، آلياً من مصدر الملف نفسه** (لا عدّ يدوي): يثبت أن كل
   واحدة من الـ17 دالة المعروفة تستدعي `_runGuarded('اسمها', ...)` فعلياً،
   أن عدد نداءات `_runGuarded(` بالملف يساوي 17 بالضبط (لا أقل، لا أكثر)،
   وأن `actionLoading = true`/`actionLoading = false` الحرفيتان لا تظهران
   إلا مرة واحدة لكل منهما بكامل الملف (`true` داخل `_runGuarded`، `false`
   فقط بتصريح الحقل الابتدائي).
3. **إجراءان مختلفان لا يحجب أحدهما الآخر**: `reviewTopup` قيد التنفيذ
   (عبر `Completer` لا يُكمَل فوراً) لا يمنع `adjustUserBalance` من الوصول
   لـAPI فعلياً بنفس اللحظة، و`actionLoading` يبقى `true` حتى ينتهي
   الاثنان معاً — لا بمجرد انتهاء الأسرع منهما.
4. الاختباران القائمان أصلاً بـ`test/models/admin_provider_doublesubmit_test.dart`
   (استدعاءان متزامنان لـ`reviewTopup`/`adjustUserBalance` → طلب واحد
   فقط) يمران بلا أي تعديل — إثبات أن الانتقال للحارس المشترك سلوكياً
   مطابق تماماً لما كان يوفره الحارسان المخصَّصان القديمان.

**تحقَّق يدوياً fail-before/pass-after** (`git stash` مؤقت لـ`admin_provider.dart`
فقط، ملف الاختبار الجديد يبقى بالشجرة): بدون الإصلاح، فشل حقيقي — 4 من
أصل 6 اختبارات فشلت فعلياً (اختبارا اكتمال التغطية: `Expected: <17>,
Actual: <0>`؛ اختبار عدم كتابة `actionLoading` مباشرة: `Expected: <1>,
Actual: <17>`؛ اختبار الإجراءين المختلفين: `actionLoading` عاد `false`
بينما `reviewTopup` لا يزال قيد التنفيذ — الخلل الكامن المذكور أعلاه، لم
يكن مُصلَحاً بعد بالكود القديم). المجموعة الكاملة: **322 اختباراً ناجحاً**
(كانت 316، +6)، لا تراجع.

## نطاق متروك عمداً
لا اختبار end-to-end حقيقي عبر شاشات الأدمن الفعلية (Widget tests كاملة
لكل شاشة تستهلك هذه الطرق الـ17) — نفس حدود `admin_provider_doublesubmit_test.dart`
الأصلي، يعمل على مستوى `AdminProvider`/`AdminApi` المُقلَّد مباشرة.

# [FEAT-ADMINREQUESTDETAIL-CLIENT-01] شاشة تفاصيل طلب للأدمن — تصل لِـGET /admin/requests/:id فعلياً من الواجهة

## الخطورة
متوسط — البند #2 من أدوات حل النزاعات المعتمدة. الـbackend
(`FEAT-ADMINREQUESTDETAIL-01`) بنى نقطة نهاية تُرجع الطلب + كل العروض +
المحادثة كاملة بنداء واحد، لكن بلا أي طريق بالواجهة للوصولها كانت
عملياً غير موجودة — بالضبط الملاحظة التي طرحها صاحب المنتج: "أي شي مش
موجود بالواجهة، بالنسبة للأدمن (الموظف مستقبلاً) مش موجود".

## الاكتشاف
البند #2 من قرار البناء المعتمد (1 إلى 4 بالترتيب). لا اكتشاف جديد —
تنفيذ مباشر لما تقرر مسبقاً.

## الحل
- `ApiEndpoints.adminRequestDetail(id)` + `AdminApi.getRequestDetail(id)`:
  نداء GET واحد لنقطة النهاية الموجودة أصلاً بالسيرفر.
- `AdminProvider`: حقول `requestDetail`/`requestDetailLoading`/`requestDetailError`
  وطرق `loadRequestDetail(id)`/`clearRequestDetail()` — تطابق حرفياً نمط
  `userDetail` الموجود مسبقاً بنفس الملف، بلا نمط جديد.
- `AdminRequestDetailScreen` (شاشة جديدة، للقراءة فقط — لا أزرار تعديل):
  بطاقة ملخص (الخدمة/الحالة، أسماء وهواتف العميل والفني، العمولة
  المخصومة إن وُجدت)، صندوق معلومات إلغاء يظهر فقط عند `status == 'ملغي'`
  (`cancel_reason`/`cancelled_by_name`/`cancelled_at` — بالضبط ما طلبه
  صاحب المنتج صراحةً لبند #1 وينعكس هنا بالواجهة الآن)، قسم العروض،
  وقسم محادثة كاملة للقراءة فقط (نفس تحويل معاينات `[image]`/`[audio]`/`[location]`
  المستخدم أصلاً بـ`chats_screen.dart`، بلا إعادة تطبيق).
- الوصول للشاشة: الضغط على بطاقة طلب بقائمة `admin_requests_screen.dart`
  (`Material`+`InkWell` يلف الـ`Container` الموجود) — تم التحقق أن هذا
  لا يتعارض مع أزرار الإلغاء/تغيير الحالة الداخلية بنفس البطاقة (Flutter
  gesture arena يحل التضارب للعنصر الأعمق، وتم إثبات هذا باختبار مخصَّص
  لا افتراضاً).
- **لا نداء `markChatRead` من هذه الشاشة** — عرض الأدمن للمحادثة يجب ألا
  يحرّك مؤشر "مقروء" لأي من طرفي المحادثة (نفس ضمان الـbackend من
  `FEAT-ADMINREQUESTDETAIL-01`، مُتحقَّق هناك مباشرة على جدول `chat_reads`).
- `requestDetail` يُمسَح ضمن `AdminProvider.clear()` (تسجيل الخروج) — أخطر
  ما يُمسَح هناك فعلياً، لأنه يحمل محادثة خاصة كاملة بين عميل وفني، لا
  مجرد بيانات بروفايل — نفس سابقة `SEC-FIX-CHATCLEAR-01`.
- 6 مفاتيح ترجمة جديدة بـ`app_ar.arb`/`app_en.arb` (`requestLoadFailedTitle`،
  `adminRequestOffersSectionTitle`، `adminRequestChatSectionTitle`،
  `cancellationReasonLabel`، `cancelledByLabel`، `commissionChargedLabel`).

## الإثبات
- `test/models/admin_provider_request_detail_test.dart` (4 اختبارات):
  نجاح يملأ `requestDetail`، فشل يضبط `requestDetailError` ويبقي
  `requestDetail` null، `clearRequestDetail()` يصفّر الاثنين، و`clear()`
  العام يمسح `requestDetail` أيضاً.
- `test/widgets/admin_request_detail_screen_test.dart` (3 اختبارات): عرض
  كامل لطلب مكتمل (أسماء العميل/الفني، العرض، نص كلتا الرسالتين حرفياً،
  وتأكيد عدم ظهور "سبب الإلغاء" لطلب غير ملغى)، عرض طلب ملغى (سبب
  الإلغاء + من ألغى + متى تظهر جميعها)، وحالة فشل الجلب (رسالة خطأ حقيقية
  تظهر، لا شاشة فارغة).
- `test/widgets/admin_requests_navigation_test.dart` (2 اختبارين): الضغط
  على بطاقة طلب يفتح `AdminRequestDetailScreen` بالمعرّف الصحيح، والضغط
  على زر الإلغاء الداخلي **لا** يفتح شاشة التفاصيل أيضاً (لا تضارب
  إيماءات بين الـ`InkWell` الخارجي الجديد والزر الداخلي القديم).

**fail-before/pass-after**: `git stash` لكل الملفات الإنتاجية السبعة —
الاختبارات الثلاثة الجديدة فشلت فشل *تحويل* حقيقي (لا اختبار منطقي فقط):
`admin_request_detail_screen.dart` غير موجود، `AdminRequestDetailScreen`
اسم غير معروف، و`MockAdminApi.getRequestDetail` غير معرَّف — لأن
`AdminApi` بحالتها القديمة (المُخزَّنة مؤقتاً) لا تعلن الطريقة إطلاقاً.
بعد استعادة التغيير (`git stash pop`): الاختبارات التسعة تمر بالكامل،
بتشغيل مجمَّع وبتشغيل منفرد لكل ملف على حدة (للتأكد أن تشابك سطور
الإخراج بالتشغيل المجمَّع لم يخفِ فشلاً حقيقياً). المجموعة الكاملة:
**331 اختباراً ناجحاً** (8 متخطّاة، فشل واحد فقط بيئي معروف مسبقاً وغير
متعلق بهذا التغيير — `l10n_screenshot_capture_test.dart` يتطلب متغير بيئة
`ARABIC_FONT_PATHS` من خطوة تثبيت خطوط بـCI غير متوفرة بهذه البيئة).
`flutter analyze --no-fatal-infos`: صفر أخطاء، صفر تحذيرات جديدة (16
ملاحظة info موجودة أصلاً بالمستودع، غير متعلقة بهذا التغيير).

## نطاق متروك عمداً
- لا أزرار فعل بهذه الشاشة (تعديل حالة، إلغاء، إلخ) — القراءة فقط، عمداً.
  الإجراءات الفعلية تبقى بالمكانها الحالي بقائمة الطلبات (`admin_requests_screen.dart`)؛
  هذه الشاشة غرضها الوحيد إعطاء الأدمن الصورة الكاملة قبل اتخاذ قرار،
  لا مضاعفة أماكن اتخاذ القرار.
- لا ربط بالمحفظة المالية (`ledger`) هنا أيضاً — نفس القرار المتروك بـ
  `FEAT-ADMINREQUESTDETAIL-01` بالسيرفر (`ledger` بلا `request_id`)،
  والحل الجذري (إضافة العمود + الترحيل + آلية استرداد حقيقية) هو البند
  #3 التالي بترتيب البناء المعتمد.

# [FEAT-PRIVACYPOLICY-01] تصحيح سياسة الخصوصية لتطابق ما يجمعه الكود فعلياً

## الخطورة
متوسط — سياسة الخصوصية مستند قانوني يُعتمَد عليه لنشر التطبيق على Google
Play (مطلوب قبل أي إصدار إنتاج)، وأي فجوة أو مبالغة فيه ليست مجرد خطأ
توثيق — بيان زائف بمستند يوقّع عليه المستخدم فعلياً (ولو ضمنياً) عرضة
لمساءلة قانونية حقيقية، ومطابقته لنموذج Google Play's Data Safety تحديداً
شرط لقبول التطبيق أصلاً.

## الاكتشاف
طُلب صراحة "Don't invent claims — base it only on what the code does" —
قراءة الكود (لا افتراض) كشفت أن سياسة الخصوصية الموجودة مسبقاً (`docs/privacy-policy.html`
و`lib/features/settings/screens/privacy_policy_screen.dart`، متطابقتان
عمداً) دقيقة بمعظمها لكن فيها ثلاث فجوات حقيقية:

1. **صورة إيصال الدفع غير مذكورة إطلاقاً.** `POST /api/topups` (السيرفر)
   يرفع صورة إثبات دفع فعلية (`receipt_url` بجدول `topups`، مسار
   `/uploads/payments/`)، يراجعها الأدمن يدوياً (`POST /admin/topups/:id/review`)
   — بيانات حقيقية تُجمع وتُخزَّن ولم تكن مذكورة بجدول "البيانات التي نجمعها".
2. **Firebase Crashlytics غير مذكورة بقسم مشاركة البيانات مع طرف ثالث.**
   `pubspec.yaml` يعلن `firebase_crashlytics: ^5.3.0`، ومُفعَّلة فعلياً
   بـ`main.dart` (`setCrashlyticsCollectionEnabled(!kDebugMode)`،
   `recordFlutterFatalError`، `recordError` بأكثر من موقع) — القسم كان
   يذكر Firebase Cloud Messaging فقط، وكأن Crashlytics غير موجودة.
3. **"تُحذف بياناتك الشخصية نهائياً" مبالغة لا تطابق سلوك الكود.**
   `anonymizeUser` (`utils/db-helpers.js`) لا يحذف صف المستخدم ولا أي سجل
   مرتبط به — يستبدل فقط الحقول المُعرِّفة (`name`/`email`/`phone`/
   `national_number`/`avatar_url`/`city`/`areas`/`services`/`fcm_token`)
   بقيم عشوائية غير قابلة للربط، ويضبط `is_active=0`/`deleted_at`. رسائل
   المحادثة (`messages.sender_id` يبقى يشير لنفس الصف)، التقييمات، وسجل
   `ledger` تبقى كلها محفوظة كاملة — فقط لم تعد قابلة للربط بهوية حقيقية.
   "حذف نهائي" وصف غير دقيق لهذا؛ "استبدال بيانات مُعرِّفة (anonymization)"
   هو الوصف الصحيح لما يحدث فعلياً بقاعدة البيانات.

## الحل
تصحيح ثلاثتها بكلا الموقعين معاً (الصفحة العامة `docs/privacy-policy.html`
والشاشة داخل التطبيق عبر `app_ar.arb`/`app_en.arb`) — الصفحة العامة نفسها
تصرّح أنها "نفس السياسة المعروضة داخل التطبيق"، فانحراف أحدهما عن الآخر
يخلق تناقضاً بذاته:
- صف جديد بجدول البيانات: صورة إيصال الدفع (إلزامية لطلب الشحن، للفنيين).
- قسم مشاركة الأطراف الثالثة أصبح يذكر Crashlytics صراحة (تفاصيل العطل
  التقنية + معرّف الجهاز، لا محتوى شخصي).
- قسم الاحتفاظ بالبيانات أُعيد صياغته ليصف الاستبدال (anonymization) بدقة
  بدل "الحذف النهائي" — مع توضيح أن المحتوى الذي أنشأه المستخدم (رسائل،
  تقييمات، سجل محفظة) يبقى محفوظاً كسجل تاريخي غير مرتبط بالاسم بعد الحذف،
  وشروط منع الحذف الثلاثة الفعلية (طلب نشط/عرض معلَّق/شحن رصيد قيد المراجعة
  — مطابقة تماماً لثلاثة الفحوص الفعلية بـ`DELETE /me`).

## الإثبات
كل بند مُتحقَّق مباشرة من الكود المصدري لا افتراضاً: `routes/topups.routes.js`
(رفع ورابط الإيصال)، `pubspec.yaml` + `main.dart` (Crashlytics مُفعَّلة
فعلياً بالإنتاج)، و`utils/db-helpers.js:anonymizeUser` + `routes/auth.routes.js:DELETE /me`
(الحقول المُستبدَلة بالضبط، وشروط منع الحذف الثلاثة بالضبط). `flutter analyze`:
صفر أخطاء، صفر تحذيرات جديدة. `flutter test`: 331 ناجح (بلا تراجع، نفس
خط الأساس قبل هذا التغيير) — لا اختبار انحدار مخصَّص لهذا التغيير لأنه
تصحيح نص/محتوى قانوني لا سلوك برمجي قابل للاختبار الآلي.

## نطاق متروك عمداً
لا تحقّق قانوني رسمي (مراجعة محامٍ) — هذا تصحيح دقّة تقنية (مطابقة النص
لما يفعله الكود فعلياً)، لا استشارة قانونية. صاحب المنتج مسؤول عن المراجعة
النهائية قبل النشر الفعلي على Google Play.

**إضافة لاحقة — اكتُشفت نسخة ثالثة، `docs/privacy-policy.html` حُذفت.**
بعد دمج ما أعلاه، تبيَّن وجود نسخة ثالثة من نفس السياسة، حيّة وفعلية:
`sallehly` (الخادم) يخدم `public/privacy.html` على `https://sallehly.com/privacy.html`
فعلياً — رابط `canonical` مضبوط، ومربوطة بفوتر الموقع. هذه النسخة كانت قد
انحرفت عن النسختين الأخريين بالفعل (صف الرقم الوطني غائب تماماً منها، بينما
تحمل صف "محتوى تذاكر الدعم" الذي لا تحمله الأخريان) — دليل مباشر على أن
تعدّد النسخ يقود للانحراف، بالضبط كما حذّر صاحب المنتج.

القرار: `docs/privacy-policy.html` (بهذا المستودع) **حُذف نهائياً** — لم يكن
مربوطاً بأي استضافة فعلية بعد (GitHub Pages لم تُفعَّل له إطلاقاً)، بعكس
`public/privacy.html` بمستودع `sallehly` المُستضاف فعلياً ومفهرَساً. **مصدر
الحقيقة الوحيد للسياسة العامة أصبح `sallehly`/`public/privacy.html`** (راجع
DECISIONS.md بذلك المستودع لتفاصيل الإصلاحات المُطبَّقة هناك — نفس الفجوات
الثلاث زائد استرجاع صف الرقم الوطني وحل تناقض صف تذاكر الدعم). شاشة التطبيق
الداخلية (`PrivacyPolicyScreen`، هذا المستودع) تبقى نسخة ثانية شرعية (المستخدم
يحتاجها بلا اتصال إنترنت أحياناً)، **لكن يجب تحديثها يدوياً كلما تغيّر
`public/privacy.html`** — لا آلية آلية تفرض التزامن بين المستودعين، فهذا
اعتماد على انضباط يدوي مستقبلي، لا ضمانة هيكلية. أُضيف صف "محتوى تذاكر
الدعم" هنا أيضاً (كان موجوداً بالنسخة الحيّة فقط) وصُحِّح نص شرط منع الحذف
ليشمل "رصيد متبقٍّ بالمحفظة" — شرط رابع حقيقي بـ`DELETE /me` (`DELETE_ACCOUNT_BALANCE_REMAINING`)
كان مفقوداً من الإصلاح الأول أعلاه أيضاً (اكتُشف فقط عند قراءة المعالج كاملاً
لاحقاً لملف `delete-account.html`).

---

# [FEAT-DEDUP-01] مسح جذري لتكرار المحتوى/المنطق القابل للانحراف — الحل الجذري لمشكلة الخصوصية أعلاه، معمَّم على كل نمط مشابه

بعد إصلاح تعدد نسخ سياسة الخصوصية أعلاه، طلب صاحب المنتج مسحاً كاملاً لكلا
المستودعين (`sallehly` الخادم و`sallehly_app` هذا) عن كل نمط تكرار مشابه —
محتوى أو منطق موجود بأكثر من نسخة يمكن أن ينحرف بصمت. المسح (3 وكلاء بحث
مستقلين) رجّع 8 بنود مرتّبة بالأولوية؛ صاحب المنتج أمر بإصلاح الثمانية
جميعاً، بالترتيب، مع اختبار انحدار لكل بند قابل للاختبار، وتقرير كل 3 بنود.
هذا المُدخَل يوثّق البنود 1-3 (طرف العميل بهذا المستودع؛ البند 1 له مُدخَل
مقابل بمستودع الخادم لأن التغيير هناك مستقل).

## الخطورة
خطير — البند 1 تحديداً (عمولة "٢ د.أ" مكتوبة يدوياً) خطير مالياً: صحيح
بالصدفة فقط لأن كل الباقات الحالية تخصم نفس الرقم؛ أول باقة بعمولة مختلفة
تجعل الفني يرى رقماً والخادم يخصم رقماً آخر — نزاع مالي مباشر. البند 2
(الحد الأدنى لكلمة السر) متوسط: لا يمنع أحداً من الدخول فعلياً (الفخّ الذي
حُذِّر منه صراحة لم يقع)، لكن كان بإمكان تغيير ساذج أن يُقفل حسابات قديمة
خارجاً بصمت. البند 3 (LedgerScreen) متوسط: ميزة عاملة فعلياً (البيانات
والمنطق موجودان بالكامل بـ`WalletProvider`) كانت مخفية خلف شاشة "قريباً"
ثابتة — لا فقدان بيانات، لكن حرمان مستخدم من رؤية سجل ماله الفعلي.

## الاكتشاف
**البند 1 — عمولة "٢ د.أ":** `UserModel.activeCommission` كان `double`
غير قابل لـnull بقيمة افتراضية `= 2` بالمُنشئ **و**بتحليل JSON
(`?? 2` مرتين مستقلتين) — أي عمولة فعلية غائبة من استجابة الخادم كانت
تُستبدَل صامتة بالرقم 2 بدل عرض حالة "غير معروف". نفس النمط بمستودع
الخادم (`public/app.js`) بلوحة الفني: `<b>2 د.أ</b>` نص ثابت حرفياً بلا أي
علاقة بـ`me.active_commission` الفعلي المُرجَع أصلاً من `GET /api/me`.

**البند 2 — الحد الأدنى لكلمة السر:** الحد "8" مكتوب حرفياً بأربع شاشات
مستقلة (تسجيل عميل، تسجيل فني، نسيت كلمة السر، تغيير كلمة السر) بلا ثابت
مشترك. الأخطر: شاشة الدخول تحمل فحصاً محلياً منفصلاً (`< 6`) لا علاقة له
بما يفرضه الخادم فعلياً — قراءة `routes/auth.routes.js` (مستودع الخادم)
أكّدت أن مسار الدخول **لا يفرض أي حد أدنى لطول كلمة السر إطلاقاً** (فقط
حد أقصى 72 حرفاً لحماية bcrypt من DoS)، بعكس مسارات الإنشاء/إعادة
التعيين/التغيير التي تفرض `< 8` فعلياً بالخادم. رفع فحص الدخول لـ8 (بدل
حذفه) كان سيُقفل أي حساب قديم بكلمة سر أقصر — بالضبط الفخّ الذي حذّر منه
صاحب المنتج صراحة.

**البند 3 — LedgerScreen:** فحص `WalletProvider`/`WalletApi`/`LedgerModel`
أكّد أن جلب دفتر الحساب (`loadLedger()`، `GET /wallet/ledger`) يعمل بالكامل
وتستهلكه فعلياً شاشة المحفظة الرئيسية — لكن `LedgerScreen` (الشاشة
المخصَّصة لعرضه بالتفصيل) كانت `StatelessWidget` ثابتة تعرض «قريباً» دائماً،
بلا أي استدعاء لـ`WalletProvider` إطلاقاً.

## الحل
**البند 1:**
- `sallehly_app`: `UserModel.activeCommission` أصبح `double?` بلا أي قيمة
  افتراضية — `fromJson` يرجّع `null` صراحة عند غياب الحقل بدل تخمين `2`.
  `send_offer_screen.dart` تعاملت مع الحالة الجديدة: عند عمولة غير معروفة
  (ولا عروض مجانية متبقية)، لا تُمنَع الإرسال محلياً (يُترَك الخادم كمرجع
  نهائي) وتُعرَض رسالة صريحة "ستُخصَم عمولة باقتك الحالية" بدل رقم قد لا
  يطابق ما سيُخصَم فعلياً (مفتاح `commissionUnknownMessage` جديد).
- `sallehly` (الخادم): `public/app.js` لوحة الفني تعرض الآن
  `me.active_commission` الحقيقي (أو "—" إن كان `null` فعلاً) بدل النص
  الثابت "2 د.أ". لوحة الأدمن (`public/app.js:2069`، بطاقة الباقات) تُركت
  كما هي عمداً — مُعلَّمة صراحة "افتراضي"، لا تدّعي كونها قيمة حيّة لكل فني،
  فليست نفس عيب البند 1.

**البند 2:** ثابت واحد مشترك `AppConstants.minPasswordLength = 8`
(`lib/core/utils/app_constants.dart`)، تستخدمه الشاشات الأربع (إنشاء
عميل/فني، نسيت كلمة السر، تغيير كلمة السر) بدل الرقم الحرفي `8` المكرَّر.
شاشة الدخول (`login_screen.dart`): **حُذف فحص الطول محلياً بالكامل** (لا
رُفع لـ8، لا تُرك عند 6) — الفحص المحلي هنا يجب ألا يرفض ما سيقبله الخادم
نفسه؛ الفحص الوحيد المتبقي هو حقل فارغ. مفتاح `t.passwordTooShortValidation`
أصبح غير مُستخدَم بهذه الشاشة (تُرك بملف الترجمة، لم يُحذف — لا يزال
يُستخدَم فعلياً بالشاشات الأربع الأخرى تحت اسم مختلف حسب الشاشة).

**البند 3:** `LedgerScreen` أُعيدت كتابتها بالكامل كـ`StatefulWidget`
يستدعي `WalletProvider.loadLedger()` بـ`initState`، ويعرض حالات
تحميل/خطأ (مع زر إعادة محاولة)/فراغ/قائمة فعلية — بنفس نمط
`AdminLedgerScreen` الموجود مسبقاً (المرجع البصري)، لكن يستهلك
`LedgerModel` مُصنَّفة مباشرة من `WalletProvider.ledger` بدل خرائط خام.
مفاتيح ترجمة جديدة (`walletLedgerEmptyTitle/Subtitle`,
`walletLedgerLoadFailedTitle`, `walletLedgerBalanceAfterLabel`) حلّت محل
`ledgerComingSoonTitle/Subtitle` المحذوفتين (كانتا مُستخدَمتين حصراً بهذه
الشاشة، تأكَّد ذلك بحث قبل الحذف).

## الإثبات
البند 1: `test/models/user_model_test.dart` — اختبار مُحدَّث يؤكد أن غياب
`active_commission` بالـJSON يرجّع `activeCommission == null` (فشل حقيقي
قبل التعديل: الكود القديم كان يرجّع `2.0`؛ نجاح بعد التعديل). البند 2:
`test/widgets/login_screen_test.dart` — استُبدل الاختبار القديم (الذي كان
يؤكّد رفض كلمة سر من 3 أحرف محلياً بلا نداء شبكة، أي بالضبط السلوك المُراد
إزالته) باختبارين: كلمة سر قصيرة **تصل فعلياً** لـ`AuthApi.login()` (فشل
حقيقي قبل التعديل: `verify` فشل لأن `login()` لم يُستدعَ إطلاقاً؛ نجاح بعد
الحذف)، وحقل فارغ لا يزال يُرفَض محلياً بلا نداء شبكة. البند 3:
`test/widgets/ledger_screen_test.dart` (ملف جديد بالكامل) — أربعة اختبارات
(قائمة حقيقية، حالة فراغ، حالة خطأ مع إعادة محاولة فعلية، مؤشر تحميل)؛
جميعها فشلت فعلياً (fail-before) عند تشغيلها ضد الشاشة القديمة (`git stash`
لتغييرات الشاشة وملفي الترجمة)، ونجحت الأربعة بعد استرجاع التعديل
(`git stash pop`). حزمة الاختبارات الكاملة: 336 اختباراً، فشل واحد فقط
(بيئي معروف مسبقاً — خطوط عربية غير مُثبَّتة، غير متعلق بهذا التغيير)، بلا
أي انحدار جديد.

## نطاق متروك عمداً
البنود 4-8 من نفس المسح لم تُعالَج بعد بهذا المُدخَل — ستُوثَّق كل دفعة
(3 بنود) بمُدخَل مستقل عند اكتمالها، حسب طلب صاحب المنتج الصريح "تقرير كل 3
بنود". الفئة الزخرفية بالكامل (أرقام تسويقية وهمية مثل "+120 طلب"،
"+50 فني" بشاشة الهبوط، وإحصاءات لوحة الأدمن بمستودع الخادم) **تبقى كما
هي عمداً** — ليست عيباً، بل قرار منتج واعٍ يحتاج توثيقاً منفصلاً يمنع أي
تعديل مستقبلي يحوّلها لبيانات حية وهمية بالخطأ (سيُضاف بمُدخَل مخصَّص لاحقاً
مع بقية الفئات المؤجَّلة).

**قرار صاحب المنتج بخصوص `active_commission ?? 2` بالخادم:** عند مراجعة
البند 1 اكتُشفت احتياطات دفاعية إضافية (`?? 2`) بمنطق حساب العمولة الفعلي
بمستودع الخادم (`offers.routes.js`, `requests.routes.js`, `topups.routes.js`)،
متّسقة مع `DEFAULT 2` على عمود `users.active_commission` بالمخطط. صاحب
المنتج راجعها وقرر إبقاءها كما هي **موثَّقة صراحة بدل حذفها** — إزالة
احتياط غير ضار لكنه متّسق مع افتراض المخطط أخطر من إبقائه، خصوصاً لو ظهر
مستقبلاً أي مسار يُدرج صفاً بلا قيمة صريحة لهذا العمود. راجع مُدخَل
`FEAT-DEDUP-01` بمستودع `sallehly` (الخادم) للتفاصيل الكاملة.

---

# [FEAT-DEDUP-01] البند 4: عدد endpoints قديم بـREADME يقرأ كتحقّق حالي — البند 5: قائمة المحافظات تُجلَب من الخادم ثم تُتجاهَل بأربع شاشات

الدفعة الثانية (بند 4 بمستودع الخادم فقط، بند 5 بكلا المستودعين) من نفس
مسح `FEAT-DEDUP-01` الموثَّق أعلاه.

## الخطورة
منخفض للبند 4 — README توثيقي داخلي فقط، لا أثر إنتاجي، لكن رقم قديم يُقرأ
كتحقّق حالي يُضلِّل أي مطوّر يعتمد عليه لاحقاً. متوسط للبند 5 — لا فقدان
بيانات ولا نزاع مالي، لكن أي محافظة يضيفها الأدمن مستقبلاً عبر تحديث
`routes/meta.routes.js` (أو حتى ملاحظة انحراف تهجئة حقيقي كالمكتشَف هنا)
تبقى غائبة عن أربع شاشات عميل رغم أنها *تجلبها فعلياً* ثم تتجاهلها — عكس
تماماً الهدف من جلبها أصلاً (نفس فلسفة `[FIX-SERVICES-01]` للمهن، لم تُطبَّق
هنا رغم وجود البنية التحتية الكاملة لها).

## الاكتشاف
**البند 4:** `README-التقسيم.md` (مستودع الخادم) يذكر "تم التحقق آلياً إن
كل الـ 49 endpoint... موجودين بالضبط مرة وحدة" — صحيح وقت تقسيم `server.js`
(2026-07-31، الكوميت الوحيد الذي يلمس هذا الملف). عدد route handlers
الفعلي الحالي (`grep -c` على `routes/*.routes.js`): **79** — الكود نما
بنسبة تزيد عن 60% منذ تلك اللحظة بلا أي تحديث لهذا الرقم، فيقرأ الآن
كتحقّق جارٍ رغم كونه لقطة تاريخية واحدة.

**البند 5:** `routes/meta.routes.js` (الخادم) يُرجع قائمة المحافظات الـ12
الرسمية كمصفوفة حرفية ثابتة عبر `GET /meta` — هذه القائمة الحقيقية الوحيدة.
لكن: (أ) لوحة الفني/العميل بمستودع الخادم (`public/app.js`) تجلب
`state.meta.cities` فعلاً عند الإقلاع، ثم `governorateOptions()` تتجاهلها
تماماً لصالح `Object.keys(JORDAN_AREAS)` (كائن JS ثابت منفصل، نفس الأسماء
لكن مصدر مختلف). (ب) بمستودع العميل (`sallehly_app`)، أربع شاشات
(`CustomerRegisterScreen`, `TechnicianRegisterScreen`, `EditProfileScreen`,
`CreateRequestScreen`) — ثلاث منها كانت أصلاً تستدعي
`RequestsProvider.loadMeta()` (لجلب المهن حصراً، عبر `[FIX-SERVICES-01]`)
ثم تتجاهل `meta.cities` المجلوبة معها بنفس الطلب، وتعرض `AppConstants.cities`
الثابتة دائماً. الرابعة (`CustomerRegisterScreen`) لم تكن تستدعي `loadMeta()`
إطلاقاً.

**اكتشاف إضافي أثناء المعالجة:** `AppConstants.cities` و`AppConstants.areasByCity`
بمستودع العميل كانتا تكتبان "عمّان" (بشدة، 5 أحرف يونيكود) بينما الخادم
والنماذج الأخرى بكل أنحاء التطبيق (`RequestModel`, `UserModel`, بيانات
اختبار متعددة) تستخدم "عمان" (بلا شدة، 4 أحرف) — قيمتان مختلفتان حرفياً.
لو استُبدلت `AppConstants.cities` بالقيمة المجلوبة من الخادم مباشرة بلا
تصحيح هذا الانحراف، كانت ستفشل مطابقة `AppConstants.areasByCity['عمان']`
صامتة تحديداً لأكبر محافظة بالمملكة (فارغة القائمة بدل قائمة الأحياء
الكاملة) — فخّ من نفس فئة فخّ البند 2 (كلمة السر)، اكتُشف واستُبق قبل
حدوثه.

## الحل
**البند 4:** استُبدل الادّعاء بملاحظة تاريخية مؤرَّخة صراحة (2026-07-31)
تنص أن الرقم لا يُحدَّث مع نمو الكود، مع ذكر العدد الحالي وقت كتابة
الملاحظة (79) كمرجع إضافي **بنفس التحذير** — لا رقم واحد بالملف يُقرأ الآن
كتحقّق جارٍ.

**البند 5:**
- الخادم (`public/app.js`): `governorateOptions()` تستخدم الآن
  `state.meta.cities` (المجلوبة فعلياً عند الإقلاع) عند توفرها وعدم
  فراغها، مع `Object.keys(JORDAN_AREAS)` كاحتياط فقط (تعطّل `/meta` أو عدم
  اكتمال الجلب بعد) — تحقّق يدوي عبر محاكاة Node مباشرة (لا بنية اختبار
  لهذا الملف، نفس فجوة البند 1) يؤكد المسارين. `areaOptions()` (تقسيم
  مدينة→أحياء) تُركت بلا تغيير عمداً — لا معادِل خادمي لها إطلاقاً (`/meta`
  يُرجع أسماء المحافظات فقط)، فتبقى محلية بالكامل بلا "قيمة مجلوبة" لتجاهلها
  أصلاً.
- العميل: الشاشات الأربع تعرض الآن `meta?.cities ?? AppConstants.cities`
  (القيمة المجلوبة، مع الثابتة كاحتياط فقط) بدل `AppConstants.cities` دائماً.
  `CustomerRegisterScreen` أضيف لها استدعاء `loadMeta()` بـ`initState`
  (لم يكن موجوداً). `EditProfileScreen`: إزالة قيد `if (isTechnician)` عن
  استدعاء `loadMeta()` — قائمة المحافظة تُعرَض لكل مستخدم بغض النظر عن
  الدور، فلا مبرر لحصر الجلب بالفني (`loadMeta()` idempotent أصلاً، لا نداء
  API إضافي فعلي إن كانت `meta` محمَّلة مسبقاً بشاشة أخرى). `AppConstants.cities`/`areasByCity`:
  "عمّان" → "عمان" (بلا شدة) بكل الموقعين، لتطابق القيمة الفعلية من الخادم
  حرفياً — يشمل ذلك القائمة الاحتياطية نفسها، لتبقى مطابقة للقيمة الحيّة
  في كل الأحوال (عند نجاح الجلب أو فشله على حدٍّ سواء).

## الإثبات
البند 4: لا اختبار آلي — تصحيح توثيقي على ملف Markdown، لا سلوك برمجي.
البند 5 (الخادم): تحقّق يدوي بمحاكاة Node مباشرة لمنطق `governorateOptions()`
الجديد (3 حالات: قائمة مجلوبة، `meta` بلا `cities`، `cities` فارغة) —
النتائج الثلاث صحيحة، لا بنية اختبار Playwright لهذا الملف (نفس فجوة
البند 1 المُسجَّلة سابقاً). البند 5 (العميل): `test/widgets/city_dropdown_meta_source_test.dart`
(ملف جديد) — أربعة اختبارات widget، واحد لكل شاشة، تفتح قائمة "المحافظة"
فعلياً وتتحقق أن محتواها مدينة اختبار وهمية مُرجَعة حصراً عبر `MockRequestsApi.getMeta()`
(لا تظهر أي محافظة من `AppConstants.cities` الثابتة). fail-before/pass-after
عبر `git stash` لملفات الإنتاج الخمسة (أربع شاشات + `app_constants.dart`):
الاختبارات الأربعة فشلت فعلياً ضد الكود القديم (لم يُعثَر على المدينة
الوهمية إطلاقاً)، ونجحت الأربعة بعد استرجاع التعديل. `test/models/app_constants_test.dart`
حصل على اختبار جديد يمنع عودة "عمّان" بشدة. حزمة الاختبارات الكاملة:
341 اختباراً، فشل واحد فقط (بيئي معروف مسبقاً — خطوط عربية)، بلا انحدار.

## نطاق متروك عمداً
`areaOptions()`/`AppConstants.areasByCity` (تقسيم محافظة→أحياء/مناطق) تبقى
ثابتة محلياً بالكامل بكلا المستودعين — لا معادِل خادمي إطلاقاً حالياً
(`/meta` لا يُرجع تفاصيل الأحياء)، فلا "قيمة مجلوبة تُتجاهَل" لتصحيحها هنا؛
إضافة endpoint خادمي لهذا قرار نطاق أوسع (يشمل: هل الأحياء بيانات ثابتة أم
تحتاج إدارة من لوحة الأدمن؟) لم يُطلَب بهذه الجولة. البنود 6-8 لم تُعالَج
بعد بهذا المُدخَل.
